# 产品需求文档：审稿意见回复助手 ReviewResponse

> 版本：V1.2 MVP · 更新时间：2026年5月16日 · 需求负责人：YN

---

## 一、需求介绍

| 字段 | 内容 |
|------|------|
| 所属业务线 | AI 学术写作工具 |
| 需求负责人 | YN |
| 需求介绍 | 基于大语言模型，为有投稿经验的研究生和青年研究者提供审稿意见辅助回复工具。用户上传论文原文并粘贴审稿意见后，系统自动解析意见条目、生成逐条回复草稿并提供修改对照表。MVP 阶段为 Web 网页形态，核心验证三个假设：用户是否愿意使用专用工具替代 ChatGPT 做审稿回复、修改对照表功能是否足够差异化、愿意付费的用户比例是否与调研一致。 |

---

## 二、需求背景

### 2.1 需求价值

全球每年发表论文约 500 万篇，投稿量约为发表量的 2 到 3 倍，意味着每年有超过 1000 万次"收到审稿意见然后撰写回复信"的场景发生。非英语母语研究者占全球科研人员的百分之六十以上，中国研究生超 300 万。

用户调研（n=33）显示：百分之九十四的用户已经在用 ChatGPT 做审稿回复，但百分之六十七的用户认为其输出"一眼假，不敢用"。问题不在于 AI 写不出文字，而在于通用大模型没有论文上下文、不懂审稿回复的专业格式和语气规范、用户自己也无法判断生成结果的好坏。百分之七十九的用户明确表示需要一款专用工具，百分之七十愿意付费。

做审稿回复这个方向，不是跟 ChatGPT 比谁的文字生成能力更强，而是在它的盲区做深度：论文上下文感知、修改位置自动标注、学术语气内置保证、回复质量提示。

### 2.2 目标用户

**核心用户**：有第一作者或通讯作者投稿经历的硕士研究生和博士研究生，刚经历或正在经历第一次到第三次修回流程。学术写作经验不足，审稿回复信写作规范尚未建立，当前依赖 ChatGPT 加师兄师姐模板的手工拼接方案。

**次要用户**：有稳定投稿经验的博士后和青年教师，已有成熟的审稿回复习惯，对效率提升有需求但痛点不如核心用户强烈。

**非目标用户**：尚未投过稿的本科生，无修回经历，不在早期推广范围。

### 2.3 典型使用场景

**场景一：第一次投稿的硕士收到 Minor Revision**

研二学生小张投了一篇中文核心期刊，收到三周后返回的 Minor Revision，审稿意见共五条。小张打开 ChatGPT 把意见粘贴进去让它生成回复，但生成的文字"感觉很假"，也不知道回复信的格式对不对。她找到学姐要了一份过往的回复信模板，对照着改写，前前后后花了一个下午。她最担心的是"回复写得不够好，审稿人直接拒了"。

在这个场景中，ReviewResponse 的价值是：她只需上传论文和粘贴审稿意见，系统自动拆解意见、生成符合学术规范的逐条回复草稿，并告诉她每条回复的语气是否合适、修改位置是否已标注。她不需要自己会写 prompt，不需要找学姐要模板，也不需要反复纠结"写得对不对"。

**场景二：被导师反复退回修改的博士**

博三学生小李投了一篇 SCI 期刊，收到 Major Revision 共十二条审稿意见。他花了三天改完论文，又花了一天写回复信。导师看了回复信草稿不满意，让他逐条修改措辞和补充理由。他反复改了五六版，每次都要重新标注哪些修改对应论文的哪个位置。最崩溃的是改到后期已经搞不清 v3 和 v5 的区别了。

在这个场景中，ReviewResponse 的价值是：系统生成回复草稿的同时自动输出修改对照表，左栏是审稿意见要点，右栏标注论文中对应的修改段落位置。导师或合作者可以直接在表上审批，无需对着论文原文查找。

**场景三：英文期刊的回复焦虑**

研二学生小王第一次投英文期刊，审稿意见全是英文。虽然大致看得懂，但写英文回复信时完全拿不准语气——怕太随意显得不专业，又怕太正式显得僵硬。她用 DeepL 辅助写作，但翻译出来的表达有时很生硬，自己又判断不了。

在这个场景中，ReviewResponse 的价值是：系统内置学术英文回复的语气规范，生成中英对照版回复信，用户可以先确认中文内容准确无误，再查看英文表达是否地道。系统还可标注出可能导致语气问题的措辞，提醒用户斟酌。

### 2.4 竞品分析

#### 2.4.1 分析结论

1. 审稿回复辅助是一个品类空白。目前没有发现任何产品专门做"审稿意见到逐条回复"这件事。最接近的 Paperpal 在审稿回复环节仅做格式检查层面的简单校验，不生成回复内容。
2. 用户当前默认方案是 ChatGPT（使用率百分之九十四），但满意度低。两个核心缺口：一是 ChatGPT 没有论文原文上下文，用户需要在两个窗口间反复对照；二是 ChatGPT 不了解审稿回复信的格式规范和语气要求，用户需要自己设计 prompt 作文本引导。
3. 不跟 ChatGPT 比生成能力，比它做不到的事：论文段落自动匹配、学术语气内置、修改位置标注、回复质量提示。
4. 进入门槛不高（MVP 核心技术是 LLM prompt 加论文段落匹配），长期壁垒在于持续积累的审稿回复语料和用户行为数据。

#### 2.4.2 竞品方案

目前审稿回复场景不存在专用竞品。用户当前方案为 ChatGPT 加手工拼接：将审稿意见粘贴进 ChatGPT，通过手动设计的 prompt 引导生成回复，然后自己整理格式、对照论文原文逐条检查修改位置、最后参考过往模板调整语气。核心痛点是自己对照的机械劳动和自己判断质量的认知压力。

Paperpal 定位投稿前全流程平台，审稿回复仅为其二十余个功能中的小模块，功能是检查已写好的回复信格式是否合规，不生成回复内容。两者不存在直接竞争。

---

## 三、需求目标

### 3.1 需求目标

1. 验证用户是否愿意从 ChatGPT 迁移到专用工具完成审稿回复任务
2. 验证修改对照表功能是否构成足够的差异化壁垒
3. 积累首批用户的行为数据和反馈，为 V2 版本的功能决策提供依据
4. 跑通"上传到生成到审核"的完整闭环，建立可迭代的产品基线

### 3.2 衡量指标

| 指标 | 计算方式 | 目标值 |
|------|---------|--------|
| 用户满意度 | 生成结果后下发满意度评价（有用、一般、无帮助） | 有用比例不低于百分之六十 |
| 生成完成率 | 成功生成回复信的次数除以点击生成按钮的次数 | 不低于百分之九十 |
| 用户留存 | 一周内使用过两次及以上的用户数除以总用户数 | 不低于百分之三十 |
| 分享转化 | 通过分享链接访问的新用户数占总访问用户数的比例 | 不低于百分之二十 |
| 平均使用时长 | 从打开页面到关闭页面的平均停留时间 | 不低于三分钟 |

---

## 四、需求范围

### 4.1 功能总表

| # | 需求 | 模块 | 涉及终端 | 优先级 | 目的 | 调研依据 |
|---|------|------|---------|:--:|------|---------|
| F1 | **论文原文上传**：支持 PDF、Word、Markdown、TXT 四种格式上传，后端自动识别文件类型并提取全文文本；同时支持文本框中直接粘贴论文正文。上传完成后展示文件名、格式徽章和字符数统计 | 输入模块 | Web | P0 | 降低输入门槛，让用户无需离开浏览器即可完成论文文本导入 | 66.7%用户反馈需反复在论文和意见间对照，上传论文是上下文感知的前提 |
| F2 | **审稿意见输入与解析**：支持文件上传和文本粘贴两种方式。点击生成后系统调用 LLM 将审稿意见拆解为带编号的逐条问题列表，按审稿人分组，标注问题类别（文献/方法/实验/写作/格式） | 输入模块 | Web | P0 | 将非结构化的审稿意见邮件转化为结构化任务清单，让用户逐条确认和回复 | 66.7%用户期望自动解析审稿意见为逐条；用户当前手动拆分审稿邮件耗时且易遗漏 |
| F3 | **回复信生成**：基于论文全文和解析后的审稿意见，生成包含致谢开头、逐条回复、结尾致谢的完整 Markdown 回复信。每条回复包含：感谢/认可审稿意见、具体修改内容说明、论文中修改位置标注。语气谦逊专业，语言自动匹配审稿意见 | 生成模块 | Web | P0 | 为用户提供可直接使用或稍作修改的专业回复草稿，消除格式和语气的认知负担 | 63.6%用户期望生成逐条回复草稿；51.5%反馈通用AI不了解回复格式规范 |
| F4 | **修改对照表**：独立于回复信输出五列对照表（序号、审稿意见要点≤30字、修改位置、修改内容摘要≤60字、状态✓/⚠）。逐条标注审稿意见要点与论文中的对应修改位置。至少80%行包含明确章节标注 | 生成模块 | Web | P0 | 提供回复信的结构化摘要视图，方便导师/合作者快速审批，解决反复修改时版本混乱问题 | 66.7%用户期望自动生成修改对照表；场景二中的博士最需要此功能 |
| F5 | **一键复制**：回复信标签页和对照表标签页各有一个复制按钮。点击后将对应内容复制到系统剪贴板，复制成功后按钮短暂显示"✓ 已复制"反馈（1.5秒）后恢复 | 输出模块 | Web | P0 | 让用户快速将结果粘贴到投稿系统或邮件中，减少手动选择-复制操作 | 参考 Stripe API 文档复制按钮模式，成熟产品的用户预期 |
| F6 | **加载示例数据**：页面提供"加载示例数据"按钮，点击后自动填入预设示例论文（约700字，五个章节）和示例审稿意见（约350字，8条意见分属2位审稿人）。数据硬编码前端，不发起API调用 | 输入模块 | Web | P0 | 零成本体验完整流程，降低新用户"不知道填什么"的激活门槛 | 参考 ChatGPT 示例卡片、Notion 模板市场的 onboarding 模式 |
| F7 | **回复质量提示**：每条生成的回复进行三项自动检查——位置标注（是否提及章节/段落/图表/页码）、语气适宜（无过度谄媚或强硬拒绝）、内容完整（是否回应意见全部要点）。结果通过对照表状态列✓/⚠标记和回复信末尾质量摘要卡片展示 | 输出模块 | Web | P1 | 帮助用户判断AI生成结果的可信度，解决"一眼假不敢用"的信任问题 | 66.7%用户反馈AI生成内容"一眼假不敢用"；Grammarly tone detection 模式参考 |
| F8 | **结果编辑**：用户可在输出文本框内直接编辑修改生成的回复信和对照表内容。纯文本层面修改，编辑后复制的是最新内容。再次生成弹出确认对话框，警告将覆盖编辑内容 | 输出模块 | Web | P1 | AI生成内容不可能100%准确，用户需要微调措辞和补充细节 | 参考 Notion AI 的"AI草稿+人工编辑"协作模式 |
| F9 | **中英对照模式**：当审稿意见为英文时，提供中英对照版回复信，左侧中文确认内容准确性，右侧英文查看地道表达。MVP阶段不做，预留设计 | 输出模块 | Web | P2 | 降低非英语母语用户的英文写作焦虑 | 场景三用户最需要；39.4%用户反馈缺乏专业术语和学术规范约束 |

### 4.2 不做（P2及以后）

| 项目 | 原因 |
|------|------|
| 用户注册登录系统 | MVP 验证核心价值，注册体系增加开发运维成本 |
| 历史生成记录保存 | 需数据库支持，MVP 不做持久化 |
| 回复模板库 | 需积累足够用户数据 |
| 导师在线审批批注 | B端功能，C端MVP不覆盖 |
| 一键导出 Word/PDF | 复制粘贴可覆盖基础需求 |
| 移动端适配 | 学术写作主战场在桌面端 |
| 语法检查与润色 | Grammarly/Paperpal已覆盖 |
| 期刊格式适配 | 各期刊差异大，MVP做通用方案 |
| 多轮对话修改 | 单次生成-编辑-复制闭环已覆盖核心场景 |

---

## 五、需求详情

### 5.1 页面架构与状态模型

#### 5.1.1 页面拓扑图

ReviewResponse 为单页 Web 应用，不涉及页面跳转。用户在同一个页面内完成从输入到导出的全部操作。页面在不同阶段呈现不同的视觉状态，共定义 11 个屏幕状态节点（S1-S11）。

```
┌──────────────────────────────────────┐
│  S0: 初始空状态（首次访问）           │
│  - Header + 双输入卡片 + 禁用按钮     │
│  - "加载示例数据" 突出展示            │
├──────────────────────────────────────┤
│  S1: 输入就绪（内容已填入）           │
│  - 输入框有内容 + 字符计数            │
│  - 生成按钮激活（暖黑底色）           │
├──────────────────────────────────────┤
│  S2: 生成中（进度条展示）             │
│  - 按钮禁用 + 旋转图标                │
│  - 四阶段进度指示器逐阶段更新          │
│  - 输出区显示骨架屏占位                │
├──────────────────────────────────────┤
│  S3: 结果展示-回复信标签              │
│  - 标签页切换可用                      │
│  - 回复信 Markdown 渲染               │
│  - 质量摘要卡片（如有警告）            │
│  - 复制按钮 + 满意度评价               │
├──────────────────────────────────────┤
│  S4: 结果展示-修改对照表标签           │
│  - 五列表格渲染                       │
│  - ✓/⚠ 状态标记                       │
│  - 复制按钮可见                        │
├──────────────────────────────────────┤
│  S5: 结果展示-质量报告标签             │
│  - 三维度质量摘要                      │
│  - 逐条详细检查结果                    │
├──────────────────────────────────────┤
│  S6: 编辑模式                         │
│  - 输出区可编辑                        │
│  - "已编辑"徽章显示                    │
├──────────────────────────────────────┤
│  S7a-S7g: 各错误状态                  │
│  - 对应不同的错误提示和恢复操作         │
└──────────────────────────────────────┘
```

#### 5.1.2 布局栅格系统

页面采用 12 列栅格布局，最大宽度 1060px，水平居中。

| 区域 | 占据列数 | 说明 |
|------|:------:|------|
| Header | 12 列 | 居中，Logo + 副标题 |
| 输入区-论文卡片 | 6 列 | 左半边 |
| 输入区-审稿意见卡片 | 6 列 | 右半边 |
| 操作栏 | 12 列 | 次按钮（左）+ 主按钮（右）|
| 进度指示器 | 12 列 | 生成中可见，4 列内嵌 4 步 |
| 输出区-标签导航 | 12 列 | 3 个标签（回复信/对照表/质量报告）|
| 输出区-内容 | 12 列 | 标签对应的内容面板 |
| 输出区-操作 | 12 列 | 复制按钮（右对齐）|
| 反馈区 | 12 列 | 满意度评价（居中）|
| Footer | 12 列 | 特色说明（居中）|

#### 5.1.3 垂直节奏规则

| 间距 | 值 | 应用场景 |
|------|-----|---------|
| 区域间 | 40px | Header→输入区、输入区→操作栏、操作栏→输出区、输出区→反馈区 |
| 区域内 | 24px | 卡片内 padding、标签内容区 padding |
| 紧密元素间 | 16px | 按钮间、标签与内容间、反馈按钮间 |
| 标签-字段间 | 8px | section-label 与下方组件 |

### 5.2 核心用户流程

#### 5.2.1 流程一：首次体验流程（加载示例 → 生成 → 审核 → 复制）

| 步骤 | 用户动作 | 系统行为 | 屏幕状态 |
|:--:|---------|---------|:--:|
| 1 | 打开页面 | 展示空状态，两个输入框显示 placeholder，"生成回复信"按钮禁用（40%透明度），"加载示例数据"按钮正常可点击 | S0 |
| 2 | 点击"加载示例数据" | 预设论文填入左侧文本框（约700字），预设审稿意见填入右侧文本框（约350字）。字符计数更新。"生成回复信"按钮激活 | S1 |
| 3 | 点击"生成回复信" | 按钮切换为加载态（置灰+旋转图标+"正在生成..."），不可点击。输出区显示进度指示器，4个阶段依次推进 | S2 |
| 4 | 等待生成完成（15-45s） | 进度指示器4步全部✓完成。页面自动平滑滚动到输出区。默认展示"回复信"标签页，回复信内容以 Markdown 渲染 | S3 |
| 5 | 阅读回复信，切换到"修改对照表"标签 | 标签切换动画（200ms 下划线滑动），对照表五列表格渲染 | S4 |
| 6 | 查看"质量报告"标签 | 三维度质量摘要卡片渲染 | S5 |
| 7 | 点击"复制回复信"按钮 | Clipboard API 写入。按钮切换为"✓ 已复制"（1.5秒）后恢复 | S3 |
| 8 | 点击满意度"👍 有用" | 三个按钮同时置灰不可点击，选中项高亮。埋点上报 | S3 |

#### 5.2.2 流程二：自主使用流程（上传论文 + 粘贴意见 → 生成 → 编辑 → 复制）

| 步骤 | 用户动作 | 系统行为 | 屏幕状态 |
|:--:|---------|---------|:--:|
| 1 | 拖拽论文 PDF 到左侧上传区 | 拖入时虚线边框变实线（#d6d3d1→#78716c），背景变#f5f5f0。解析完成后文本框填入全文，上方显示格式徽章"PDF"+字符数 | S1 |
| 2 | 在右侧粘贴审稿意见 | 字符计数实时更新。生成按钮已激活 | S1 |
| 3 | 点击"生成回复信" | 同流程一步骤3-4 | S2→S3 |
| 4 | 在回复信中直接修改措辞 | 文本框获得焦点，光标出现。右上角出现"已编辑"徽章（12px，琥珀底色#fef3c7） | S6 |
| 5 | 再次点击"生成回复信" | 弹出确认对话框："再次生成将覆盖您的编辑内容，是否继续？" + "取消"(次按钮) + "继续生成"(主按钮) | S6 |
| 6 | 点击"继续生成" | 对话框关闭，重新生成，覆盖编辑内容。"已编辑"徽章消失 | S2→S3 |

#### 5.2.3 流程三：错误恢复流程

| 错误场景 | 触发条件 | 系统行为 | 用户可执行操作 |
|---------|---------|---------|--------------|
| 论文为空 | 点击生成时 paper_content 无内容 | 论文卡片边框变红，卡片下方显示红色提示文字，输出区不变化，页面滚动到论文卡片 | 粘贴论文或上传文件 |
| 论文过短 | 点击生成时 len(paper)<100 | 论文卡片边框变琥珀色，卡片下方显示提示，输出区不变化 | 补充更多论文内容或忽略警告继续（不阻断） |
| 审稿意见为空 | 点击生成时 review_text 无内容 | 审稿意见卡片边框变红，卡片下方显示红色提示文字 | 粘贴审稿意见或上传文件 |
| 文件解析失败 | 上传了损坏的 PDF 或非文本文件 | 文本框填入错误提示："[解析失败] 无法读取文件内容，请尝试手动粘贴文本"，上传区文件卡片变红 | 手动粘贴文本 |
| API超时 | 生成超过60秒无响应 | 进度指示器停在当前步骤，显示"⚠️ 生成超时，请重试或缩短论文长度" | 点击"重试"或缩短输入 |
| API错误 | DeepSeek API 返回错误 | 进度指示器停在当前步骤，显示"⚠️ 服务暂时不可用，请稍后重试" | 点击"重试" |

#### 5.2.4 流程四：二次生成流程

用户首次生成后编辑了内容，然后修改了输入（如增删论文段落或更换审稿意见），再次点击生成。

核心差异点：
1. 如果用户已编辑输出内容 → 弹出确认对话框
2. 如果用户未编辑 → 直接覆盖生成，无确认
3. "已编辑"的判断：用户是否在文本框中进行过任何字符变更

---

由于文档篇幅巨大，我将分批完成。先提交已完成的前半部分，继续写后半部分。

先完成 5.3（功能详细规格 F1-F9），这是最核心的部分。

### 5.3 功能详细规格

---

#### 5.3.1 F1：论文原文上传 [P0]

##### 5.3.1.1 功能概述

用户通过文件上传或文本粘贴将论文全文输入系统。这是后续所有生成流程的数据基础——没有论文上下文，LLM 无法标注修改位置、无法给出具体引用。支持 PDF（pdfplumber 优先，降级 PyPDF2）、Word（python-docx）、Markdown、TXT 四种格式，覆盖绝大多数学术写作场景。

##### 5.3.1.2 用户故事

- 作为一位刚收到审稿意见的硕士生，我希望直接把期刊系统下载的 PDF 拖进页面，系统自动提取文字，不用我手动复制粘贴各章节。
- 作为一位用 Overleaf 写作的博士，我希望粘贴 LaTeX 编译后的纯文本或直接上传 Markdown 文件，系统都能正确解析。

##### 5.3.1.3 验收条件

- [ ] 拖拽 .pdf 文件到上传区，3 秒内文本填入下方 textarea，上方显示格式徽章"PDF"和字符数
- [ ] 拖拽 .docx 文件，正确提取文字（不包括页眉页脚）
- [ ] 拖拽 .md 文件，保留原始 Markdown 文本
- [ ] 拖拽 .txt 文件，UTF-8 正确解码
- [ ] 拖拽 .png 文件，上传区抖动 + 错误提示"不支持的文件格式"
- [ ] 文件解析失败时，文本框填入错误提示，不阻断手动粘贴
- [ ] 用户手动在 textarea 中粘贴文本，字符计数实时更新
- [ ] 字符数 < 100 时，计数器显示红色
- [ ] 字符数 > 50000 时，计数器显示琥珀色，显示黄色警告条但不阻断
- [ ] 文本为空时，计数器不显示

##### 5.3.1.4 涉及 UI 组件

| 组件 | 类型 | 位置 |
|------|------|------|
| 文件上传区 | gr.File | 论文卡片顶部 |
| 文本输入框 | gr.Textbox (lines=13) | 文件上传区下方 |
| 字符计数器 | gr.HTML（动态更新） | 文本输入框右下角 |
| 格式徽章 | gr.HTML（动态更新） | 文件上传区右上角 |

##### 5.3.1.5 组件状态详表

**文件上传区：**

| 状态 | 视觉规格 | 触发条件 | 退出条件 |
|------|---------|---------|---------|
| 默认 | 虚线边框 1.5px dashed #d6d3d1, 背景 #fafaf8, 圆角 10px, padding 20px, 文字"拖拽文件到此处或点击上传", 图标📤 | 组件初始加载 | 用户开始拖拽文件 |
| 拖拽悬停 | 边框变为实线 #78716c, 背景 #f5f5f0, 轻微 scale 1.01（200ms ease） | 文件拖入热区 | 文件离开热区→默认；文件释放→解析中 |
| 解析中 | 文件卡片显示文件名 + 旋转图标 + "正在解析...", 上传区整体不可交互 | 文件释放 | 解析成功→已解析；解析失败→解析失败 |
| 已解析 | 文件卡片显示：文件图标 + 文件名(截断>30字) + 格式徽章(PDF蓝/DOCX紫/MD绿/TXT灰) + 字符数 + ×移除按钮 | 解析成功 | 用户点×→默认；上传新文件→解析中 |
| 解析失败 | 文件卡片红边, 错误图标, 错误信息"无法解析文件，请尝试手动粘贴", ×移除 | 解析失败 | 用户点×→默认 |
| 格式错误 | 上传区抖动动画（translateX ±4px, 300ms）, 红色提示"不支持的文件格式，请上传 PDF/Word/Markdown/TXT" | 释放了非支持格式文件 | 2秒后提示消失→默认 |

**文本输入框：**

| 状态 | 视觉规格 | 触发条件 | 退出条件 |
|------|---------|---------|---------|
| 空/默认 | 背景 #fafaf8, 边框 #e7e5e4, placeholder 可见（#a8a29e） | 初始加载 | 获得焦点或有内容 |
| 聚焦 | 边框 #a8a29e, 外围光晕 3px rgba(28,25,23,0.06) | 点击或 Tab 进入 | 失焦 |
| 有内容 | 同默认, 字符计数器显示（右下角） | 文本框非空 | 内容清空→空 |
| 有内容-警告 | 计数器琥珀色 #d97706, 下方黄色提示条"论文字符数超过50000，生成可能截断部分内容" | 字符数 > 50000 | 内容减少到50000以下→有内容 |
| 错误 | 边框红色 #dc2626, 下方红色提示文字 | 点击生成时内容为空或<100字符 | 用户修改内容→失焦恢复 |

**字符计数器：**

| 状态 | 视觉 | 触发条件 |
|------|------|---------|
| 隐藏 | display:none | 文本框为空 |
| 正常 | 12px, #a8a29e, 文字"N 字符" | 文本框非空且 N<50000 |
| 警告 | 12px, #d97706, 加粗, 文字"N / 50000 字符" | N ≥ 50000 |

##### 5.3.1.6 交互规格

**拖拽上传完整序列：**
1. 用户从文件管理器拖拽 PDF → 鼠标进入浏览器窗口（无反馈）→ 鼠标进入上传热区（边框变色，150ms ease）
2. 用户释放文件 → 上传区短暂"接收"脉冲（scale 1.0→1.02→1.0, 200ms bounce）
3. 文件卡片出现，显示"正在解析..."（旋转图标 + 文件名）
4. 后端 extract_text() 执行（<3s）
5. 成功：文件卡片切换为"已解析"态（格式徽章弹入，字符数淡入，200ms）；下方 textarea 内容更新（文本直接填入，不带动画）
6. 失败：文件卡片切换为"解析失败"态（红边 + 错误信息，200ms）

**手动粘贴序列：**
1. 用户点击 textarea → 聚焦态（边框变色 150ms）
2. 用户粘贴文本 → 内容即时填入，失焦后计数器淡入（150ms）
3. 字符数 < 100 时计数器为红色 → 用户继续输入 → ≥100 时计数器变回灰色（即时）

##### 5.3.1.7 边界条件

1. **论文 0 字符**：点击生成时拦截，红色提示"请先上传论文或粘贴正文"
2. **论文 1-99 字符**：点击生成时拦截，红色提示"论文内容太短（少于100字），请上传完整论文"
3. **论文 50001+ 字符**：允许生成，但显示黄色警告不阻断。后端截断至前 8000 字符送入 LLM
4. **文件名含特殊字符**（如 `论文(最终版)V3_张三.pdf`）：正常解析，文件名显示 URL-encode 后截断
5. **PDF 含扫描图片无文字层**：pdfplumber 提取为空，降级 PyPDF2 也失败 → 解析失败提示
6. **Word 含大量表格/公式**：python-docx 提取文本 + 表格文字，公式显示为 [公式] 占位
7. **Markdown 含代码块**：代码块内容保留，不执行语法高亮
8. **多次上传同一文件**：每次重新解析，覆盖当前 textarea 内容

##### 5.3.1.8 错误处理

| 错误 | 触发条件 | 用户可见文案 | 用户操作 | 系统恢复 | 阻断？ |
|------|---------|------------|---------|---------|:--:|
| E1-1 格式不支持 | 上传.jpg/.png/.xlsx等 | "不支持的文件格式，请上传 PDF、Word、Markdown 或 TXT 文件" | 移除文件，重试正确格式 | 上传区恢复默认 | 是 |
| E1-2 解析失败 | PDF损坏/加密/纯图片 | "[解析失败] 无法读取文件内容。请尝试：1) 手动复制论文正文粘贴到下方文本框 2) 将 PDF 另存为文本后重新上传" | 手动粘贴文本 | textarea 填入错误提示（可编辑） | 否 |
| E1-3 文件为空 | 上传了文件大小0KB的文件 | "[解析失败] 文件内容为空，请检查文件" | 重新上传 | 上传区恢复默认 | 是 |
| E1-4 论文为空 | 点击生成时无内容 | "请先上传论文或粘贴正文" | 上传文件或粘贴 | - | 是 |
| E1-5 论文过短 | 点击生成时<100字符 | "论文内容太短（少于100字），请确保上传了完整的论文正文（至少包含摘要、引言、方法等核心章节）" | 补充内容 | - | 是 |

##### 5.3.1.9 文案清单

| 位置 | 中文文案 | 预留英文 |
|------|---------|---------|
| 上传区占位 | 拖拽文件到此处或点击上传 | Drag & drop or click to upload |
| 上传区格式说明 | 支持 PDF、Word、Markdown、TXT 格式 | Supports PDF, Word, Markdown, TXT |
| 已解析提示 | 已解析 {format}，共 {N} 字符 | Parsed {format}, {N} chars |
| 解析中 | 正在解析... | Parsing... |
| 字符计数 | {N} 字符 | {N} chars |
| 字符超限警告 | 论文字符数超过50000，生成时可能截断部分内容 | Paper exceeds 50,000 characters; content may be truncated |
| 格式错误 | 不支持的文件格式，请上传 PDF、Word、Markdown 或 TXT 文件 | Unsupported format. Please upload PDF, Word, Markdown, or TXT |

##### 5.3.1.10 可访问性

- 文件上传区支持键盘操作：Tab 聚焦 + Enter/Space 打开文件选择器
- 上传区 aria-label="上传论文文件，支持 PDF、Word、Markdown、TXT 格式"
- 解析状态通过 aria-live="polite" 播报（"文件解析完成，共12345字符"）
- 错误状态通过 aria-live="assertive" 播报
- 字符计数区域 aria-live="off"（避免频繁播报）

##### 5.3.1.11 竞品参考

| 竞品 | 做法 | 我们借鉴 | 我们差异化 |
|------|------|---------|-----------|
| **ChatGPT** | 文件上传按钮 + 粘贴区，上传后显示文件名，无格式徽章 | 上传解析反馈（格式徽章+字符数） | 多格式统一解析（ChatGPT 对 PDF 解析不稳定） |
| **Stripe** | 文件拖拽区虚线边框，hover 时实线+背景变色，上传后文件卡片 | 拖拽交互三态（默认/悬停/已上传），文件卡片设计 | 增加格式徽章和字符数统计 |
| **Grammarly** | 无文件上传，直接粘贴 URL 或文本 | 不适用 | 学术场景需要多格式文件支持 |

---

#### 5.3.2 F2：审稿意见输入与解析 [P0]

##### 5.3.2.1 功能概述

用户通过文件上传或文本粘贴将审稿意见输入系统。点击"生成回复信"后，系统首先调用 LLM 解析审稿意见：识别不同审稿人（Reviewer 1/2/3...）、将意见拆分为逐条编号的问题列表、标注每条意见的问题类别（文献/方法/实验/写作/格式）。解析结果在右侧"审稿意见解析"输出区展示，用户可以核对拆分是否正确。

##### 5.3.2.2 用户故事

- 作为收到 Major Revision（12条意见，2位审稿人）的博士，我希望系统自动拆解每条意见、按审稿人分组，我不需要手动编号和分组。
- 作为收到中文审稿意见的研究者，我希望系统正确识别中文意见的条目边界，不要把一句话拆成两条。

##### 5.3.2.3 验收条件

- [ ] 审稿意见为空时点击生成，拦截并提示"请先粘贴审稿意见"
- [ ] 英文审稿意见含"Reviewer 1:"前缀 → 正确识别并分组
- [ ] 审稿意见为自然段落形式（无编号）→ LLM 正确拆分为逐条
- [ ] 审稿意见已编号 → 保留原编号，补充分组和类别标注
- [ ] 解析结果按审稿人分组展示，每组包含该审稿人的所有意见
- [ ] 每条意见标注类别（literature / method / experiment / writing / minor）
- [ ] 无最低字符限制（允许<50字的简短意见）

##### 5.3.2.4 涉及 UI 组件

与 F1 共用论文卡片对侧的审稿意见卡片，组件类型相同：gr.File + gr.Textbox + 字符计数器。

**审稿意见解析输出区（与当前不同，需新增）：**

将当前版本中的"审稿意见解析"输出框从纯文本 Textbox 改为 Markdown 组件，渲染格式化的解析结果。

##### 5.3.2.5 组件状态详表

审稿意见输入卡片的组件状态与 F1 论文上传卡片基本一致。差异点：
- 无最低字符限制（不触发"内容太短"警告）
- 无字符数上限警告（审稿意见通常不超过10000字）

解析输出区状态：

| 状态 | 视觉 | 触发条件 |
|------|------|---------|
| 默认 | 显示 placeholder "审稿意见将自动拆分为逐条编号，按审稿人分组展示" | 初始加载/无生成结果 |
| 有结果 | Markdown 渲染：标题"共N条意见，分属M位审稿人"，按分组展示 | 生成成功 |

##### 5.3.2.6 交互规格

解析不是独立操作，是"生成回复信"流程的第一步。详见 5.4.1 生成进度指示器。

##### 5.3.2.7 边界条件

1. **审稿意见只有一句话**（如"直接接收"）：解析为1条，类别标注为"无需修改"
2. **审稿意见含编号但不连续**（如"1. xxx 3. xxx 5. xxx"）：LLM 按实际条目重新编号
3. **审稿意见含混合语言**（中英混杂）：按主要语言判定，保留原文措辞
4. **审稿意见含附件引用**（如"see attached file"）：标注为"含附件引用，请自行查看"
5. **审稿意见为纯中文**：正确解析中文条目边界
6. **审稿意见超过 5000 字**：仍然解析，不截断

##### 5.3.2.8 错误处理

| 错误 | 触发条件 | 用户可见文案 | 用户操作 | 阻断？ |
|------|---------|------------|---------|:--:|
| E2-1 审稿意见为空 | 点击生成时 review_text 为空 | "请先粘贴审稿意见。将期刊或会议的审稿意见邮件原文粘贴到右侧输入框" | 粘贴或上传 | 是 |
| E2-2 解析失败 | LLM 返回乱码或极短内容 | "⚠️ 审稿意见解析异常，请检查意见格式后重试" | 检查输入，重试 | 是 |
| E2-3 未识别到审稿人 | 意见中无 Reviewer 1/2 标记 | 默认按1位审稿人处理，所有意见归为一组 | 无操作 | 否 |

##### 5.3.2.9 文案清单

| 位置 | 中文文案 |
|------|---------|
| 输入区 placeholder | 支持 PDF、Word、Markdown 或纯文本上传，也可以直接粘贴审稿意见原文。\n\n可以是：期刊审稿邮件、投稿系统导出意见、导师批注等。 |
| 解析结果标题 | 共 {N} 条意见，分属 {M} 位审稿人 |
| 审稿人分组标题 | Reviewer {n}（{category_counts}） |
| 类别标签-文献 | 文献 |
| 类别标签-方法 | 方法 |
| 类别标签-实验 | 实验 |
| 类别标签-写作 | 写作 |
| 类别标签-格式 | 格式 |

##### 5.3.2.10 可访问性

- 解析结果标题使用 h3 语义标签
- 每条意见使用列表项 li
- 类别标签使用 aria-label="{类别}类问题"

##### 5.3.2.11 竞品参考

| 竞品 | 做法 | 借鉴 | 差异化 |
|------|------|------|--------|
| ChatGPT | 用户自己设计 prompt 引导拆解，无内置解析 | 无 | 内置自动解析 + 分组 + 分类 |
| Paperpal | 无审稿意见输入功能 | - | - |
| Scholarcy | 文献摘要自动提取要点 | 结构化提取模式 | 针对审稿意见格式优化 |

---

#### 5.3.3 F3：回复信生成 [P0]

##### 5.3.3.1 功能概述

这是产品的核心生成功能。系统以论文全文和解析后的审稿意见为输入，调用 LLM 生成一份完整的符合学术规范的回复信。回复信为 Markdown 格式，包含：开头致谢段落 → 按审稿人分组的逐条回复 → 结尾致谢。每条回复遵循"感谢/认可 → 具体修改说明 → 论文位置标注"三要素结构。语气谦逊专业，语言自动匹配审稿意见的主要语言。

##### 5.3.3.2 用户故事

- 作为一位收到 Minor Revision 的硕士生，我希望生成的回复信直接能用——有致谢开头、逐条回复、致谢结尾，我只需微调措辞就可以发给期刊。
- 作为一位英文不太好的博士，我希望生成的英文回复信语气得体专业，不会因为用词不当被审稿人认为态度不好。

##### 5.3.3.3 验收条件

- [ ] 生成的回复信包含开头致谢段落（感谢编辑和审稿人）
- [ ] 生成的回复信包含结尾致谢段落
- [ ] 逐条回复按审稿人分组，每组有 Reviewer 标题
- [ ] 每条回复包含：引用原意见 + 感谢/认可 + 修改说明 + 位置标注
- [ ] 中文审稿意见 → 中文回复信；英文审稿意见 → 英文回复信
- [ ] 回复信为 Markdown 格式，标题、列表、加粗正确渲染
- [ ] 生成失败时显示错误提示和重试引导

##### 5.3.3.4 涉及 UI 组件

| 组件 | 类型 | 位置 |
|------|------|------|
| 生成按钮 | gr.Button (primary, lg) | 操作栏右侧 |
| 回复信内容区 | gr.Markdown | 输出区"回复信"标签页 |
| 回复信编辑区 | gr.Textbox (可切换) | 编辑模式下替换 Markdown |

##### 5.3.3.5 组件状态详表

**生成按钮：**

| 状态 | 视觉规格 | CSS | 触发条件 | 交互 |
|------|---------|-----|---------|------|
| 默认-禁用 | 透明度 40%, 无阴影, 文字"生成回复信" | opacity:0.4; pointer-events:none; | 论文为空或审稿意见为空 | 不可点击 |
| 默认-激活 | 底色 #1c1917, 白色文字, 阴影 0-1-3-12%, padding 14px 32px, 圆角 10px, 字号 16px, 字重 600 | 见 CUSTOM_CSS button.primary | 两个输入框均非空 | 可点击 |
| 悬停 | 底色 #292524, 上浮 1px, 阴影加深 0-4-14-18% | transform:translateY(-1px); box-shadow:... | 鼠标进入热区 | cursor:pointer |
| 聚焦 | 外围光晕 3px rgba(28,25,23,0.10) | :focus-visible | Tab 聚焦 | 按 Enter/空格 触发 |
| 按下 | 底色 #171412, 下沉 1px, 阴影减弱 | transform:translateY(0); | mousedown | release→加载中 |
| 加载中 | 置灰 #a8a29e, 文字"正在生成...", 旋转图标(左侧), 不可点击 | cursor:not-allowed; | API 调用开始 | 不可点击 |
| 成功后恢复 | 同默认-激活 | - | API 返回成功 | 可再次点击 |

**回复信内容区：**

| 状态 | 视觉 | 触发条件 |
|------|------|---------|
| 空/占位 | 12px placeholder "点击「生成回复信」后，AI 将在此生成完整的逐条回复草稿..." | 尚未生成 |
| 骨架屏 | 3条灰色脉冲条，模拟段落布局 | 生成中（S2） |
| 内容渲染 | 完整 Markdown 渲染，h2-h4 标题层级，有序/无序列表，加粗/斜体 | 生成完成 |
| 编辑态 | Textbox 替换 Markdown，显示原始 Markdown 文本 | 用户点击"编辑" |

##### 5.3.3.6 交互规格

**生成触发与等待：**
1. 用户点击"生成回复信" → 按钮进入加载态（150ms 过渡）
2. 输出区显示 4 阶段进度指示器（详见 5.4.1）
3. 进度指示器第3阶段"正在生成回复草稿"持续最久（10-25s）
4. 全部4阶段完成后，回复信内容以 Markdown 渲染淡入（opacity 0→1, 300ms ease）
5. 页面平滑滚动到输出区顶部（scroll-behavior:smooth, 500ms）

**生成结果结构示例：**

```markdown
尊敬的编辑和审稿人：

感谢您们在百忙之中审阅我们的稿件，并提出了宝贵而富有建设性的修改意见...

---

### Reviewer 1

**Comment 1:** The literature review in Section 2 is somewhat outdated...

**Response 1:** 感谢审稿人的宝贵意见。我们已在第2节中补充了2024-2025年的最新文献，新增了6篇参考文献...

**修改位置：** 第2节第3-5段；新增参考文献[32]-[37]

---

（重复所有意见...）

---

再次感谢编辑和审稿人的时间和指导。我们相信修改后的稿件在质量上有了显著提升...
```

##### 5.3.3.7 边界条件

1. **论文+审稿意见总字符数超过 LLM 上下文窗口**：截断论文至前8000字符，在质量报告中标注"论文部分内容未纳入生成上下文"
2. **审稿意见超过16条**：仍然生成全部回复，但总 token 可能达到上限（4000），截断时在末尾标注
3. **审稿意见含非标准字符**（如化学式、数学符号）：保留原始符号，不做转换
4. **审稿意见语言混合**（中英夹杂）：以主要语言（>70%）决定回复语言
5. **论文标题未提供**：默认使用"论文"替代

##### 5.3.3.8 错误处理

| 错误 | 触发条件 | 用户可见文案 | 用户操作 | 阻断？ |
|------|---------|------------|---------|:--:|
| E3-1 API Key 未配置 | .env 文件无 DEEPSEEK_API_KEY | "⚠️ API 密钥未配置。请检查 .env 文件中的 DEEPSEEK_API_KEY 设置" | 配置 API Key 后重试 | 是 |
| E3-2 API 超时 | 生成超过 60s 无响应 | "⚠️ 生成超时。请尝试缩短论文长度后重试" | 缩短输入、重试 | 是 |
| E3-3 API 返回错误 | DeepSeek 返回 4xx/5xx | "⚠️ 服务暂时不可用（{错误码}），请稍后重试" | 等待后重试 | 是 |
| E3-4 生成结果截断 | max_tokens 不够，输出末尾不完整 | "⚠️ 生成结果可能不完整（审稿意见较多时偶发），请检查最后一条回复是否完整。如不完整可缩短论文后重试" | 检查、缩短输入重试 | 否 |

##### 5.3.3.9 文案清单

| 位置 | 文案 |
|------|------|
| 生成按钮-默认 | 生成回复信 |
| 生成按钮-加载中 | 正在生成... |
| 回复信标签 | 回复信 Response Letter |
| 空状态 placeholder | 点击「生成回复信」后，AI 将在此处生成完整的逐条回复草稿，包含致谢开头、逐条回复与结尾致谢 |
| Reviewer 分组标题 | **Reviewer {N}**（共{N}条意见） |

##### 5.3.3.10 可访问性

- 生成按钮 aria-label="生成审稿意见回复信"（默认）/ "正在生成回复信，请稍候"（加载中）
- 回复信使用语义化 Markdown 标题层级（h2 致谢, h3 Reviewer, h4 每条回复）
- 生成完成时 aria-live="polite" 播报"回复信生成完成，共{N}条回复"

##### 5.3.3.11 竞品参考

| 竞品 | 做法 | 借鉴 | 差异化 |
|------|------|------|--------|
| **ChatGPT** | 对话式生成，非结构化，需手动拼装格式 | 流式输出感受 | 内置学术回复信标准模板，自动按审稿人分组 |
| **Paperpal** | 仅检查已有回复信格式 | - | 直接生成完整的结构化回复信 |
| **Claude** | 可上传文件+长上下文，但用户仍需手动 prompt 引导格式 | 大上下文窗口的思路 | 专门优化审稿回复场景的 prompt 工程 |

---

#### 5.3.4 F4：修改对照表 [P0]

##### 5.3.4.1 功能概述

独立于回复信，输出一份五列结构化对照表。每一行对应一条审稿意见，列分别为：序号、审稿意见要点（≤30字摘要）、修改位置（章节/段落/图表编号）、修改内容摘要（≤60字）、状态（✓已完成/⚠待改进）。用户可在"修改对照表"标签页中查看，也可一键复制粘贴到邮件或文档中供导师审批。

这是 ReviewResponse 差异化的核心功能——用户调研中 66.7% 的用户期望自动生成修改对照表，且目前市场上没有任何产品提供此功能。

##### 5.3.4.2 用户故事

- 作为一位需要导师审批的博士，我希望有一份独立的修改对照表，导师可以直接在上面查看每条意见的对应修改位置，不需要对着论文原文逐一查找。
- 作为一位收到12条意见的研究者，我希望能快速扫一眼所有修改的完成状态，确认没有遗漏任何一条意见。

##### 5.3.4.3 验收条件

- [ ] 对照表为五列：序号、审稿意见要点、修改位置、修改内容摘要、状态
- [ ] 行数与审稿意见解析条数一致
- [ ] 至少 80% 的行在"修改位置"列包含明确的章节或页码标注
- [ ] "状态"列使用 ✓（已完成）和 ⚠（待改进）标记
- [ ] "审稿意见要点"列每条 ≤30 字
- [ ] "修改内容摘要"列每条 ≤60 字
- [ ] 对照表可在标签页中独立查看，也可通过复制按钮整体复制

##### 5.3.4.4 涉及 UI 组件

| 组件 | 类型 | 位置 |
|------|------|------|
| 标签导航 | gr.Tabs | 输出区顶部 |
| 对照表标签面板 | gr.Markdown | "修改对照表"标签页内 |
| 复制按钮 | gr.Button (secondary, sm) | 对照表右下角 |

##### 5.3.4.5 组件状态详表

**对照表内容区：**

| 状态 | 视觉 | 触发条件 |
|------|------|---------|
| 空/占位 | 12px placeholder "修改对照表将在生成完成后在此展示，包含序号、意见要点、修改位置、修改摘要、状态五列" | 尚未生成 |
| 骨架屏 | 5行灰色脉冲条模拟表格行 | 生成中 |
| 内容渲染 | Markdown 表格渲染，每行交替浅灰背景（zebra striping），✓ 绿色 #16a34a，⚠ 琥珀色 #d97706 | 生成完成 |

**状态单元格：**

| 值 | 视觉 | 含义 |
|:--:|------|------|
| ✓ | 绿色 #16a34a, 加粗, 前有空格 | 三项检查均通过（位置标注 + 语气适宜 + 内容完整）|
| ⚠ | 琥珀色 #d97706, 加粗, 悬停显示 tooltip 列出未通过的检查项 | 至少一项检查未通过 |

##### 5.3.4.6 交互规格

**对照表切换：**
1. 用户点击"修改对照表"标签 → 标签下划线从当前位置滑动到目标标签（200ms cubic-bezier 0.4-0-0.2-1）
2. 内容区交叉淡入淡出（150ms opacity）
3. URL hash 更新为 `#changes`（如技术可行）
4. 键盘快捷键：Ctrl+2 切换到对照表标签

**对照表内容格式示例：**

| # | 审稿意见要点 | 修改位置 | 修改内容摘要 | 状态 |
|:--:|------------|---------|------------|:--:|
| 1 | 文献综述缺少2024-2025年文献 | 第2节第3-5段 | 新增6篇transformer相关最新文献 | ✓ |
| 2 | 跨模态注意力机制描述不够详细 | 第3节第2段 | 补充数学公式和计算流程说明 | ✓ |
| 3 | 遮挡条件未量化描述 | 第4.1节 | 新增遮挡等级定义表（Table 3） | ⚠ |
| 4 | 缺少统计显著性检验 | 第4.2节 | 补充5次重复实验的均值±标准差 | ✓ |
| 5 | 基线方法过旧 | 第4.2节 | 新增VideoMAE和UniFormer对比 | ⚠ |
| 6 | 缺少推理时间测量 | 第4.3节（新增） | 新增FPS测量结果（Table 5） | ✓ |
| 7 | 引言段落过长 | 第1节第2-4段 | 重组为更短的段落，每段一个要点 | ✓ |
| 8 | Figure 3缺失 | 第4节末尾 | 已插入Figure 3 | ✓ |

##### 5.3.4.7 边界条件

1. **审稿意见只有1条**：对照表只有1行，仍展示完整五列
2. **审稿意见超过20条**：表格渲染可能超出视口，允许垂直滚动
3. **某条意见无对应修改位置**：修改位置列填入"已在回复信中说明（未修改论文）"
4. **修改跨越多个位置**：修改位置列用分号分隔，如"第2节第3段；第5节第1段"
5. **状态全部为✓**：质量摘要卡片显示"所有修改项均已通过质量检查"（绿色）
6. **有⚠项**：质量摘要卡片显示"共{N}项需要注意"（琥珀色）

##### 5.3.4.8 错误处理

| 错误 | 触发条件 | 用户可见文案 | 用户操作 | 阻断？ |
|------|---------|------------|---------|:--:|
| E4-1 对照表生成不完整 | 行数与意见条数不一致 | "⚠️ 对照表可能不完整（识别{N1}条意见但生成{N2}行），请检查" | 检查、重试 | 否 |
| E4-2 修改位置标注率低 | <80% 行包含位置标注 | "⚠️ 仅{N}%的修改项包含位置标注，建议在回复信中补充具体位置" | 手动补充 | 否 |

##### 5.3.4.9 文案清单

| 位置 | 文案 |
|------|------|
| 对照表标签 | 修改对照表 Change Table |
| 表头-# | # |
| 表头-意见要点 | 审稿意见要点 |
| 表头-修改位置 | 修改位置 |
| 表头-修改摘要 | 修改内容摘要 |
| 表头-状态 | 状态 |
| 空状态 placeholder | 修改对照表将在生成完成后在此展示，包含序号、意见要点、修改位置、修改摘要、状态五列 |

##### 5.3.4.10 可访问性

- 表格使用语义化 `<table>` 标签
- 表头使用 `<th scope="col">`
- ⚠ 单元格使用 aria-label="需要注意：{未通过的检查项}"
- 表格支持 Tab 键逐格导航

##### 5.3.4.11 竞品参考

| 竞品 | 做法 | 借鉴 | 差异化 |
|------|------|------|--------|
| **GitHub PR Review** | 评论与代码行关联，状态标记（resolved/unresolved） | 意见与修改位置关联、状态标记模式 | 学术场景的五列结构化摘要 |
| **Notion Database** | 表格视图、状态字段、筛选排序 | 表格 Zebra striping、状态 badge | 自动生成而非手动填写 |
| **Paperpal** | 无此功能 | - | 品类首创 |

---

#### 5.3.5 F5：一键复制 [P0]

##### 5.3.5.1 功能概述

回复信标签页和修改对照表标签页各有一个独立的复制按钮。点击后将对应标签页的完整内容（用户编辑后的最新版本）复制到系统剪贴板。复制成功后按钮短暂显示"✓ 已复制"反馈（1.5秒）后恢复原状。浏览器不支持 Clipboard API 时按钮置灰并提示手动 Ctrl+C。

##### 5.3.5.2 用户故事

- 作为一位需要将回复信粘贴到投稿系统的用户，我希望一键复制全部内容，不需要手动选择全部文字再 Ctrl+C。
- 作为一位需要单独复制对照表的用户，我希望两个标签页有独立的复制按钮。

##### 5.3.5.3 验收条件

- [ ] 回复信标签页右下角有"复制回复信"按钮（secondary/sm）
- [ ] 对照表标签页右下角有"复制对照表"按钮（secondary/sm）
- [ ] 点击后 Clipboard API 写入剪贴板
- [ ] 复制成功后按钮切换为"✓ 已复制"（绿色图标+文字）
- [ ] 1.5 秒后按钮恢复原状
- [ ] 浏览器不支持 Clipboard API → 按钮置灰 + tooltip "请手动 Ctrl+C 复制"
- [ ] 复制的是编辑后的最新内容
- [ ] 无生成结果时按钮不可见

##### 5.3.5.4 涉及 UI 组件

| 组件 | 类型 | 位置 |
|------|------|------|
| 复制按钮×2 | gr.Button (secondary, sm) | 各标签页右下角 |

##### 5.3.5.5 组件状态详表

| 状态 | 视觉 | 触发条件 | 退出条件 |
|------|------|---------|---------|
| 默认 | 白底 #ffffff, 边框 #e7e5e4, 文字"复制回复信/复制对照表", 📋图标, 字号 14px, padding 10px 20px | 生成完成，有可复制内容 | - |
| 悬停 | 背景 #fafaf8, 边框 #d6d3d1 | 鼠标进入热区 | 鼠标离开→默认 |
| 聚焦 | 外围光晕 3px rgba(28,25,23,0.06) | Tab 聚焦 | 失焦→默认 |
| 按下 | 背景 #f5f5f0, 下沉 1px | mousedown | mouseup→复制中 |
| 复制成功 | 背景 #f0fdf4(浅绿), 边框 #86efac, 文字"✓ 已复制", 绿色✓图标, 轻微 scale 脉冲 | Clipboard API 返回成功 | 1.5s 后→默认 |
| 复制失败 | 背景 #fef2f2(浅红), 边框 #fecaca, 文字"复制失败", 红色✗图标 | Clipboard API 返回失败 | 2s 后→默认 |
| 禁用-无API | 置灰 40%, 文字"请手动 Ctrl+C 复制", tooltip 图标 | 浏览器不支持 navigator.clipboard | 始终（无恢复） |
| 隐藏 | display:none | 无生成结果 | 生成成功后→默认 |

##### 5.3.5.6 交互规格

**复制成功序列：**
1. 用户点击复制按钮 → 按钮按下态（瞬时）
2. `navigator.clipboard.writeText(content)` 调用
3. 成功：按钮背景过渡到浅绿（150ms ease），图标切换为 ✓，文字切换为"已复制"，轻微弹跳（scale 1.0→1.05→1.0, 200ms bounce）
4. 1.5 秒后：按钮背景过渡回白色（150ms ease），图标恢复 📋，文字恢复

**快速连续点击：**
- 每次点击重置 1.5s 计时器
- 防抖：点击后 300ms 内不接受二次点击

##### 5.3.5.7 边界条件

1. **用户编辑了输出内容后复制**：复制的是编辑后的最新内容
2. **复制内容包含 Markdown 标记**：复制原始 Markdown 文本（包括 `*` `#` `|` 等标记），不复制渲染后的富文本
3. **内容极长（>10000字）**：Clipboard API 可能超时 → 降级为选中全部文本并提示 Ctrl+C

##### 5.3.5.8 错误处理

| 错误 | 触发条件 | 用户可见 | 用户操作 |
|------|---------|---------|---------|
| E5-1 Clipboard API 不可用 | 非 HTTPS 环境或旧浏览器 | 按钮置灰，文案"请手动 Ctrl+C 复制" | 手动选择复制 |
| E5-2 剪贴板写入失败 | 浏览器权限拒绝 | 按钮短暂变红"复制失败" | 重试或手动复制 |
| E5-3 无内容可复制 | 生成失败，输出区为空 | 按钮隐藏 | 先生成结果 |

##### 5.3.5.9 文案清单

| 位置 | 中文 | 英文 |
|------|------|------|
| 复制回复信按钮 | 复制回复信 | Copy Response Letter |
| 复制对照表按钮 | 复制对照表 | Copy Change Table |
| 复制成功反馈 | ✓ 已复制 | ✓ Copied |
| 复制失败反馈 | 复制失败，请重试 | Copy failed, retry |
| 无API提示 | 请手动 Ctrl+C 复制 | Press Ctrl+C to copy |

##### 5.3.5.10 可访问性

- 复制按钮 aria-label="复制回复信内容到剪贴板"
- 复制成功后 aria-live="polite" 播报"已复制到剪贴板"
- 按钮支持 Ctrl+C 快捷键（监听全局键盘事件，但仅当对应标签页激活时生效）

##### 5.3.5.11 竞品参考

| 竞品 | 做法 | 借鉴 | 差异化 |
|------|------|------|--------|
| **Stripe API 文档** | 代码块右上角复制按钮，点击后✓反馈1.5s恢复 | 1.5s反馈时长、✓图标、浅绿背景 | 两个标签页独立按钮 |
| **GitHub** | 代码块复制按钮，Copied! 反馈 | 复制反馈模式 | - |
| **ChatGPT** | 每条回复底部复制按钮 | 按钮位置（右下角） | - |

---

#### 5.3.6 F6：加载示例数据 [P0]

##### 5.3.6.1 功能概述

页面提供"加载示例数据"按钮，点击后在论文输入框和审稿意见输入框中同时填入预设内容。用户无需准备任何内容即可体验完整的产品流程。数据硬编码在前端，不发起 API 调用。可重复点击，每次覆盖已有内容。

##### 5.3.6.2 用户故事

- 作为一位第一次访问的潜在用户，我想先看看这个工具能生成什么效果，再决定是否上传自己的论文。
- 作为一位想测试工具边界的研究者，我想用预设数据快速验证不同场景下的生成效果。

##### 5.3.6.3 验收条件

- [ ] 点击"加载示例数据"后，论文和审稿意见同时填入
- [ ] 示例论文约 600-800 字，含摘要、引言、方法、实验、结论五个章节
- [ ] 示例审稿意见约 300-500 字，8 条意见分属 2 位审稿人
- [ ] 直接覆盖已有内容，不弹确认框
- [ ] 不发起 API 调用（纯前端操作）
- [ ] 点击后"生成回复信"按钮激活
- [ ] 可重复点击
- [ ] 按钮位于"生成回复信"按钮左侧

##### 5.3.6.4 涉及 UI 组件

| 组件 | 类型 | 位置 |
|------|------|------|
| 加载示例按钮 | gr.Button (secondary, sm) | 操作栏左侧 |
| 论文输入框 | gr.Textbox | 按钮点击后填入 |
| 审稿意见输入框 | gr.Textbox | 按钮点击后填入 |

##### 5.3.6.5 组件状态详表

| 状态 | 视觉 | 触发条件 |
|------|------|---------|
| 默认 | 白底 #ffffff, 边框 #e7e5e4, 文字"加载示例数据", 字号 14px, 字重 500 | 页面初始加载 |
| 悬停 | 背景 #fafaf8, 边框 #d6d3d1 | 鼠标进入热区 |
| 按下 | 背景 #f5f5f0 | mousedown |
| 加载中 | 按钮内部旋转图标 + 文字不变 | 用户连续快速点击时防抖（300ms） |

##### 5.3.6.6 交互规格

1. 用户点击"加载示例数据" → 按钮短暂按下态（100ms）
2. 论文输入框和审稿意见输入框同时填入内容（无动画，直接替换）
3. 字符计数器分别更新
4. "生成回复信"按钮从禁用态切换为激活态（150ms）
5. 如已有内容 → 直接覆盖，无确认对话框（用户可重复点击回退到示例）

##### 5.3.6.7 边界条件

1. **用户已填入自己的论文和意见** → 覆盖，不弹确认。如需恢复，用户需手动重新输入
2. **用户已生成结果** → 只覆盖输入区，输出区保持上次结果不变
3. **用户快速连击** → 300ms 防抖，仅执行一次

##### 5.3.6.8 文案清单

| 位置 | 文案 |
|------|------|
| 按钮文字 | 加载示例数据 |

##### 5.3.6.9 可访问性

- 按钮 aria-label="加载示例论文和审稿意见数据"

##### 5.3.6.10 竞品参考

| 竞品 | 做法 | 借鉴 |
|------|------|------|
| **ChatGPT** | 首页示例卡片（"Plan a trip""Write an email"） | 预设内容让用户零成本体验 |
| **Notion** | 模板市场，一键使用模板 | 一键填入预设数据 |
| **Canva** | 模板预览，hover 查看效果 | - |

---

#### 5.3.7 F7：回复质量提示 [P1]

##### 5.3.7.1 功能概述

对每条生成的回复进行三项自动检查：（1）**位置标注**——是否提及章节、段落、图表或页码；（2）**语气适宜**——无过度谄媚（如"深感抱歉""非常羞愧"）或强硬拒绝（如"审稿人显然错了"）；（3）**内容完整**——是否回应了审稿意见的全部要点。检查结果通过对照表"状态"列的 ✓/⚠ 标记和回复信末尾的"质量摘要"卡片展示。LLM 在生成回复的同时输出质量检查结果，不额外调用 LLM。

##### 5.3.7.2 用户故事

- 作为一位不确定 AI 生成内容质量的硕士生，我希望系统能告诉每条回复哪里可能有问题，帮我判断哪些需要修改。
- 作为一位需要提交导师审核的博士，我希望有一份质量摘要，导师可以快速了解回复信的整体质量。

##### 5.3.7.3 验收条件

- [ ] 每条回复检查三项：位置标注、语气适宜、内容完整
- [ ] 三项全部通过 → 对照表状态列显示 ✓
- [ ] 至少一项未通过 → 对照表状态列显示 ⚠
- [ ] 回复信末尾展示"质量摘要"卡片，包含三维度的通过/未通过统计
- [ ] 质量检查随回复信生成同步完成，不额外调用 LLM
- [ ] 提示使用温和措辞（如"建议补充"而非"错误"）

##### 5.3.7.4 涉及 UI 组件

| 组件 | 类型 | 位置 |
|------|------|------|
| 状态标记 ✓/⚠ | 表格单元格内 | 对照表第5列 |
| ⚠ Tooltip | 悬停弹出浮层 | ⚠标记上方 |
| 质量摘要卡片 | Markdown 渲染 | 回复信末尾 |
| 质量摘要卡片 | 可选：HTML card组件 | 回复信标签页底部 |

##### 5.3.7.5 组件状态详表

**质量摘要卡片：**

| 状态 | 视觉 | 触发条件 |
|------|------|---------|
| 全部通过 | 浅绿边框 #bbf7d0, 浅绿背景 #f0fdf4, 标题"✓ 质量检查全部通过", 三维度各显示通过数和总数 | 所有回复三项检查均通过 |
| 部分警告 | 琥珀色边框 #fcd34d, 浅琥珀背景 #fffbeb, 标题"⚠ 共{N}项需要注意", 三维度各显示通过数和总数 | 至少一项检查未通过 |
| 隐藏 | display:none | 尚未生成或无质量检查数据 |

**质量摘要卡片内容格式：**

```
## 质量摘要

| 检查维度 | 结果 | 说明 |
|---------|:--:|------|
| 位置标注 | 7/8 | 第3条回复缺少具体章节标注 |
| 语气适宜 | 8/8 | 全部通过 |
| 内容完整 | 6/8 | 第5条回复未完全回应意见中关于"统计检验"的要求；第8条回复仅部分回应 |
```

##### 5.3.7.6 交互规格

**Tooltip 交互：**
1. 用户鼠标悬停在 ⚠ 标记上 → tooltip 浮层淡入（150ms），显示未通过的检查项
2. 用户鼠标移开 → tooltip 淡出（100ms）
3. Tooltip 内容示例："⚠ 缺少位置标注：回复中未提及修改在论文中的具体章节或页码"
4. Tooltip 最大宽度 280px, 白底, 阴影, 圆角 8px, 字号 12px, 三角箭头指向 ⚠

##### 5.3.7.7 边界条件

1. **LLM 未输出质量检查结果**（解析失败）：降级显示"质量检查暂不可用"，不阻断
2. **审稿意见只有格式问题**（如"请调整图表大小"）：位置标注检查标准放宽，不要求论文正文位置
3. **所有回复均无位置标注**：质量摘要卡片显示严重警告，建议用户在回复信中补充

##### 5.3.7.8 错误处理

| 错误 | 触发条件 | 用户可见 | 用户操作 |
|------|---------|---------|---------|
| E7-1 质量检查数据缺失 | LLM 未按预期格式输出检查结果 | 质量摘要卡片显示"质量检查暂不可用" | 自行判断质量 |
| E7-2 检查结果与意见数不匹配 | 检查条目 ≠ 意见条目 | 质量摘要标注"检查结果不完整" | 手动检查缺失项 |

##### 5.3.7.9 文案清单

| 位置 | 文案 |
|------|------|
| 全部通过标题 | ✓ 质量检查全部通过 |
| 部分警告标题 | ⚠ 共 {N} 项需要注意 |
| 检查维度-位置标注 | 位置标注 |
| 检查维度-语气适宜 | 语气适宜 |
| 检查维度-内容完整 | 内容完整 |
| Tooltip-缺位置 | 缺少位置标注：回复中未提及修改在论文中的具体章节或页码 |
| Tooltip-语气问题 | 语气可能需要调整：{具体问题} |
| Tooltip-内容不完整 | 可能未完全回应审稿意见的全部要点 |

##### 5.3.7.10 可访问性

- ⚠ 标记 aria-label="需要注意：{未通过检查项}"
- 质量摘要卡片使用 aria-live="polite" 播报摘要

##### 5.3.7.11 竞品参考

| 竞品 | 做法 | 借鉴 |
|------|------|------|
| **Grammarly** | 逐句标注问题类型，侧边栏展示整体质量评分和分类 | 质量维度分类 + 逐条标记 |
| **Paperpal** | 按类别展示检查结果，颜色标记严重程度 | 通过/警告二值标记 |
| **Hemingway Editor** | 可读性评分 + 颜色高亮不同类型问题 | 温和的提示风格 |

---

#### 5.3.8 F8：结果编辑 [P1]

##### 5.3.8.1 功能概述

用户可在输出文本框内直接编辑修改生成的回复信和对照表内容。纯文本层面的修改（不支持富文本），编辑后复制的是最新内容。再次点击"生成回复信"时弹出确认对话框，警告将覆盖编辑内容。

##### 5.3.8.2 用户故事

- 作为一位需要微调措辞的研究者，我希望直接在生成结果上修改，不需要复制到外部编辑器。
- 作为一位需要在对照表中补充细节的用户，我希望对照表内容也可编辑。

##### 5.3.8.3 验收条件

- [ ] 输出内容区域可编辑（interactive=True）
- [ ] 编辑后右上角出现"已编辑"徽章（琥珀底色 #fef3c7）
- [ ] 再次点击"生成回复信" → 弹出确认对话框
- [ ] 确认对话框：标题"覆盖编辑内容"，说明"再次生成将覆盖您的编辑内容，是否继续？"，按钮"取消"(secondary) + "继续生成"(primary)
- [ ] 点击"取消"关闭对话框，保留编辑内容
- [ ] 点击"继续生成"覆盖编辑内容，"已编辑"徽章消失
- [ ] 纯文本层面修改，不支持富文本格式

##### 5.3.8.4 涉及 UI 组件

| 组件 | 类型 | 位置 |
|------|------|------|
| 输出文本框 | gr.Textbox (interactive=True) | 各标签页内容区 |
| "已编辑"徽章 | gr.HTML | 输出区右上角 |
| 确认对话框 | gr.HTML（模态层） | 页面中央 |

##### 5.3.8.5 组件状态详表

**确认对话框：**

| 状态 | 视觉 | 触发条件 |
|------|------|---------|
| 隐藏 | display:none | 默认 |
| 显示 | 页面遮罩 rgba(0,0,0,0.3), 居中白色卡片, 圆角 16px, padding 24px, 标题 16px/600 | 有编辑内容且用户点击"生成回复信" |

**"已编辑"徽章：**

| 状态 | 视觉 | 触发条件 |
|------|------|---------|
| 隐藏 | display:none | 未编辑或刚生成 |
| 显示 | 12px, 琥珀背景 #fef3c7, 琥珀文字 #92400e, 圆角 6px, padding 2px 8px, 文字"已编辑" | 用户对文本框进行了修改 |

##### 5.3.8.6 交互规格

**编辑检测：**
- 比较当前文本框内容与生成时的原始内容（通过 gr.State 存储原始值）
- 内容不同 → 徽章显示
- 内容恢复为原始值 → 徽章消失

**覆盖确认流程：**
1. 用户点击"生成回复信" → 系统检测"已编辑"状态
2. 已编辑 → 弹出确认对话框（遮罩 fade in 200ms, 卡片 scale 0.95→1.0 bounce 300ms）
3. 用户点击"继续生成" → 对话框消失 → 原始值更新 → 徽章消失 → 重新生成
4. 用户点击"取消" → 对话框消失 → 保留编辑 → 不生成
5. 用户点击遮罩外区域 → 等同于"取消"
6. 用户按 Escape → 等同于"取消"

##### 5.3.8.7 边界条件

1. **用户编辑后切换标签页**：编辑内容保留，"已编辑"徽章跟随标签页
2. **用户编辑后复制**：复制的是编辑后的最新内容
3. **用户编辑后加载示例数据（只改输入不改输出）**：输出区编辑内容保留
4. **用户清空所有编辑内容恢复原始值**：徽章消失，再次生成不弹确认

##### 5.3.8.8 文案清单

| 位置 | 文案 |
|------|------|
| 已编辑徽章 | 已编辑 |
| 确认对话框标题 | 覆盖编辑内容 |
| 确认对话框正文 | 再次生成将覆盖您在回复信和对照表中所做的编辑内容，是否继续？ |
| 取消按钮 | 取消 |
| 继续按钮 | 继续生成 |

##### 5.3.8.9 可访问性

- 确认对话框使用 role="alertdialog"
- 对话框打开时焦点自动移到"取消"按钮
- Escape 关闭对话框
- 对话框显示时背景内容 aria-hidden="true"

##### 5.3.8.10 竞品参考

| 竞品 | 做法 | 借鉴 |
|------|------|------|
| **Notion AI** | AI 生成后内容可编辑，支持"接受""拒绝""继续编辑" | AI 草稿+人工编辑协作模式 |
| **ChatGPT** | 编辑 prompt 后重新生成，覆盖之前结果（无确认） | "覆盖前确认"是我们的差异化 |
| **Google Docs** | 版本历史，可回溯每次编辑 | MVP 不实现版本历史，但预留方向 |

---

#### 5.3.9 F9：中英对照模式 [P2，MVP 不做]

##### 5.3.9.1 功能概述

当审稿意见为英文时，提供中英对照版回复信。左侧展示中文回复信（确认内容准确性），右侧展示英文回复信（确认表达地道性）。系统检测审稿意见语言后自动决定是否开启对照模式。MVP 阶段不做，预留设计决策和接口。

##### 5.3.9.2 预留设计

- **触发条件**：审稿意见主要语言为英文（英文占比 > 70%）
- **展示方式**：左右分栏，中文在左（60%宽度），英文在右（40%宽度）
- **生成方式**：后端一次 LLM 调用同时输出中英双语结果，不额外调用
- **产品化时机**：V2，待 V1 验证核心价值后启动
- **预留数据字段**：`response_letter_cn`, `response_letter_en`

---

### 5.4 通用交互模式规范

#### 5.4.1 生成进度指示器（参考：Vercel 部署进度）

**模式描述**：用户点击"生成回复信"后，输出区展示一个四阶段步骤条，每阶段独立显示 pending / in-progress / done / failed 状态。降低等待焦虑，提供透明度。

**阶段定义：**

| 阶段 | 名称 | 预计时长 | 状态说明 |
|:--:|------|:------:|---------|
| 1 | 正在解析审稿意见 | 3-8s | 调用 parse_review_points() |
| 2 | 正在匹配论文段落 | 5-10s | 分析论文结构，定位各意见对应段落 |
| 3 | 正在生成回复草稿 | 10-25s | 调用 generate_response_letter()，最耗时 |
| 4 | 正在整理修改对照表 | 2-5s | 组装对照表 + 质量检查 |

**每阶段视觉状态：**

| 状态 | 图标 | 颜色 | 文字样式 | 说明 |
|------|:--:|------|---------|------|
| pending | ○ 空心圆 | #d6d3d1 | 正常字重，灰色 | 尚未开始 |
| in-progress | ◉ 旋转动画 | #1c1917 | 加粗，暖黑色 | 当前进行中 |
| done | ✓ 实心对勾 | #16a34a | 正常字重，绿色 | 已完成 |
| failed | ✗ 叉号 | #dc2626 | 加粗，红色 | 此阶段失败 |

**进度指示器 CSS 布局：**
- 四个步骤水平排列，步骤间用 1px 线段连接（#e7e5e4）
- 每个步骤：图标（24px）+ 下方文字（13px）
- 总宽度自适应（最大 600px），居中
- 当前进行中的步骤有微弱的呼吸动画（opacity 0.8→1.0 循环, 1.5s）

**降级处理：**
- 如果生成很快（<10s），进度指示器仍完整展示每个阶段的 ✓ 完成标记
- 如果某阶段失败，停在当前步骤，显示失败状态和重试按钮

#### 5.4.2 标签页切换（参考：Notion database view 切换）

**模式描述**：输出区使用三标签页组织（回复信/修改对照表/质量报告）。活跃标签下方有 2px 滑动下划线指示器。

**交互规格：**
- 默认展示"回复信"标签页（生成完成后自动激活）
- 点击其他标签 → 下划线从当前位置滑动到目标位置（200ms, cubic-bezier 0.4-0-0.2-1）
- 内容区交叉淡入淡出（opacity 0→1, 150ms ease）
- 键盘快捷键：Ctrl+1 回复信, Ctrl+2 对照表, Ctrl+3 质量报告
- URL hash 更新：`#response`, `#changes`, `#quality`（可选）

**标签样式：**

| 状态 | 视觉 |
|------|------|
| 未选中 | 文字 #78716c, 无下划线, 字号 14px, 字重 400, padding 8px 16px |
| 悬停 | 文字 #292524, 无下划线（下划线仅当前激活标签有） |
| 选中 | 文字 #1c1917, 字重 600, 底部 2px 实线 #1c1917 |
| 聚焦 | 外围光晕（同按钮focus） |

#### 5.4.3 一键复制（参考：Stripe API 文档复制按钮）

已在 F5 中详细描述。核心参数：
- 反馈持续时间：1.5 秒（基于 Stripe 实践：<1s 用户感知不到，>2s 卡顿感）
- 成功色：浅绿 #f0fdf4
- 图标切换：📋 → ✓
- 防抖：300ms

#### 5.4.4 内联编辑（参考：Notion block 编辑）

已在 F8 中详细描述。核心参数：
- 输出区默认 interactive=True
- 编辑检测：对比当前内容与原始生成内容的 hash
- "已编辑"徽章：12px, 琥珀背景 #fef3c7, 圆角 6px

#### 5.4.5 质量提示徽章（参考：Grammarly tone detection）

已在 F7 中详细描述。核心参数：
- ✓ 绿色 #16a34a，⚠ 琥珀色 #d97706
- Tooltip 出现延迟 300ms（避免误触），消失延迟 100ms
- Tooltip 最大宽度 280px

#### 5.4.6 满意度反馈（参考：ChatGPT thumbs）

**模式描述**：输出区底部展示满意度评价，三个按钮（👍有用 / 😐一般 / 👎无帮助），互斥单选。点击 👎 展开可选反馈文本框。

**交互规格：**
1. 初始状态：三个按钮等间距排列，默认样式（白底+边框）
2. 点击任一按钮 → 三个按钮同时置灰（不可重复评价），选中项保持高亮（暖黑底色+白字）
3. 选中时：选中项 scale 脉冲（1.0→1.1→1.0, 120ms ease-out）
4. 如果选中 👎：反馈文本框从高度 0 展开到 80px（250ms ease-out），placeholder "请告诉我们哪里可以做得更好...（选填，限200字）"
5. 用户在文本框中输入（<200字）→ 内容自动保存
6. 文本框失焦 → 内容保持，按钮区显示"感谢反馈 ✓"（2秒后恢复为置灰不可评价态）
7. 评价结果写入后端日志

**视觉规格：**

| 状态 | 👍按钮 | 😐按钮 | 👎按钮 |
|------|--------|--------|--------|
| 默认 | 白底+灰边 | 白底+灰边 | 白底+灰边 |
| 悬停 | 背景 #fafaf8 | 背景 #fafaf8 | 背景 #fafaf8 |
| 选中 | 暖黑底+白字 | 暖黑底+白字 | 暖黑底+白字 |
| 置灰（已评价） | opacity 0.5 | opacity 0.5 | opacity 0.5 |

#### 5.4.7 渐进披露（参考：Linear issue 创建）

**模式描述**：默认展示最简化界面（两个输入框 + 生成按钮），复杂功能按需展开。

**应用：**
- 首次访问：两个空输入框 + 禁用的生成按钮 + 突出的"加载示例数据"按钮
- 输入就绪：生成按钮激活，字符计数出现
- 生成完成后：输出区、复制按钮、质量摘要、满意度评价逐步可见
- 无"高级设置"折叠面板（MVP 无高级设置）

#### 5.4.8 文件拖拽上传（参考：Stripe 文件上传）

已在 F1 中详细描述。核心参数：
- 默认：虚线边框 #d6d3d1
- 悬停：实线 #78716c, 背景 #f5f5f0
- 错误：抖动动画（±4px, 300ms）

---

### 5.5 组件状态矩阵总表

#### 5.5.1 按钮组件

| 组件 | 状态 | 底色 | 文字色 | 边框 | 阴影 | 字重 | 交互 | 触发 |
|------|------|------|--------|------|------|------|------|------|
| 主按钮(lg)-默认激活 | #1c1917 | #fff | #1c1917 | 0-1-3-12% | 600 | pointer | 输入就绪 |
| 主按钮(lg)-悬停 | #292524 | #fff | #292524 | 0-4-14-18% | 600 | pointer, translateY(-1px) | hover |
| 主按钮(lg)-按下 | #171412 | #fff | #171412 | 0-1-2-8% | 600 | translateY(0) | mousedown |
| 主按钮(lg)-加载中 | #a8a29e | #fff | #a8a29e | none | 600 | not-allowed, 旋转图标 | API调用中 |
| 主按钮(lg)-禁用 | #1c1917(opacity0.4) | #fff(opacity0.4) | none | none | 600 | not-allowed | 输入为空 |
| 次按钮(sm)-默认 | #fff | #57534e | #e7e5e4 | none | 500 | pointer | - |
| 次按钮(sm)-悬停 | #fafaf8 | #57534e | #d6d3d1 | none | 500 | pointer | hover |
| 次按钮(sm)-按下 | #f5f5f0 | #57534e | #d6d3d1 | none | 500 | translateY(0) | mousedown |
| 复制按钮-默认 | #fff | #57534e | #e7e5e4 | none | 500 | pointer | 有可复制内容 |
| 复制按钮-成功 | #f0fdf4 | #16a34a | #86efac | none | 500 | 短暂(1.5s) | 复制成功 |
| 复制按钮-失败 | #fef2f2 | #dc2626 | #fecaca | none | 500 | 短暂(2s) | 复制失败 |
| 复制按钮-隐藏 | - | - | - | - | - | display:none | 无内容 |

#### 5.5.2 输入组件

| 组件 | 状态 | 背景 | 边框 | 文字色 | placeholder | 其他 |
|------|------|------|------|--------|-------------|------|
| Textbox-默认 | #fafaf8 | #e7e5e4 | #292524 | #a8a29e | 圆角10px, pad14-16 |
| Textbox-聚焦 | #fafaf8 | #a8a29e | #292524 | - | 外围光晕3px |
| Textbox-错误 | #fef2f2 | #dc2626 | #292524 | - | 下方红色提示 |
| Textbox-警告 | #fffbeb | #d97706 | #292524 | - | 下方琥珀提示 |
| Textbox-禁用 | #f5f5f0 | #e7e5e4 | #a8a29e | - | cursor:not-allowed |

#### 5.5.3 文件上传组件

| 状态 | 边框 | 背景 | 其他 |
|------|------|------|------|
| 默认 | 1.5px dashed #d6d3d1 | #fafaf8 | 圆角10px, pad20px |
| 拖拽悬停 | 1.5px solid #78716c | #f5f5f0 | scale1.01(200ms) |
| 已上传 | 1px solid #e7e5e4 | #fff | 文件卡片显示 |
| 解析中 | 1px solid #e7e5e4 | #fff | 旋转图标+文件名 |
| 解析成功 | 1px solid #e7e5e4 | #fff | 格式徽章+字符数 |
| 解析失败 | 1px solid #fecaca | #fef2f2 | 红色错误信息 |
| 格式错误 | 1px dashed #dc2626 | #fef2f2 | 抖动动画300ms |

#### 5.5.4 显示组件

| 组件 | 状态 | 视觉规格 |
|------|------|---------|
| 标签-选中 | 文字#1c1917, 字重600, 底部2px #1c1917 |
| 标签-未选中 | 文字#78716c, 字重400 |
| 标签-悬停 | 文字#292524 |
| 进度步骤-pending | ○ #d6d3d1, 正常字重 |
| 进度步骤-in-progress | ◉ #1c1917 旋转, 加粗 |
| 进度步骤-done | ✓ #16a34a, 正常字重 |
| 进度步骤-failed | ✗ #dc2626, 加粗 |
| 状态标记-✓ | #16a34a, 16px, 加粗 |
| 状态标记-⚠ | #d97706, 16px, 加粗, tooltip |
| 质量卡片-全通过 | 边框#bbf7d0, 背景#f0fdf4 |
| 质量卡片-有警告 | 边框#fcd34d, 背景#fffbeb |
| Badge-已编辑 | 背景#fef3c7, 文字#92400e, 12px, 圆角6px |
| Tooltip | 白底, 阴影, 圆角8px, 12px, 最大280px, 三角箭头 |
| 骨架屏 | 灰色脉冲条, 3条模拟段落 |
| 确认对话框-遮罩 | rgba(0,0,0,0.3), fade200ms |
| 确认对话框-卡片 | 白底, 圆角16px, pad24px, scale bounce 300ms |
| Toast-成功 | 绿底#f0fdf4, 绿边#bbf7d0, ✓图标 |
| Toast-错误 | 红底#fef2f2, 红边#fecaca, ✗图标 |
| Toast-警告 | 琥珀底#fffbeb, 琥珀边#fcd34d, ⚠图标 |

---

### 5.6 数据流与状态管理

#### 5.6.1 前端状态机

| 状态ID | 状态名 | 输入区 | 生成按钮 | 进度条 | 输出区 | 复制按钮 | 反馈区 |
|--------|--------|:--:|:--:|:--:|:--:|:--:|:--:|
| IDLE | 初始空状态 | 空 | 禁用 | 隐藏 | 占位 | 隐藏 | 隐藏 |
| READY | 输入就绪 | 有内容 | 激活 | 隐藏 | 占位 | 隐藏 | 隐藏 |
| GENERATING | 生成中 | 锁定(不可编辑) | 加载中 | 显示(逐阶段) | 骨架屏 | 隐藏 | 隐藏 |
| DONE_RESPONSE | 结果-回复信 | 可编辑 | 激活 | 隐藏 | 回复信渲染 | 显示 | 显示 |
| DONE_TABLE | 结果-对照表 | 可编辑 | 激活 | 隐藏 | 对照表渲染 | 显示 | 显示 |
| DONE_QUALITY | 结果-质量报告 | 可编辑 | 激活 | 隐藏 | 质量报告渲染 | 隐藏 | 显示 |
| EDITED | 编辑态 | 可编辑 | 激活(覆盖确认) | 隐藏 | 编辑中+已编辑徽章 | 显示 | 显示 |
| ERROR | 错误 | 可编辑 | 激活 | 显示(失败) | 错误信息 | 隐藏 | 隐藏 |

**状态转换规则：**
- IDLE → READY：论文非空 AND len(paper) ≥ 100 AND 审稿意见非空
- READY → IDLE：任一输入被清空
- READY → GENERATING：点击"生成回复信"
- GENERATING → DONE_RESPONSE：生成成功
- GENERATING → ERROR：生成失败（API错误/超时等）
- DONE_RESPONSE ↔ DONE_TABLE ↔ DONE_QUALITY：用户切换标签页
- DONE_* → EDITED：用户编辑输出内容
- EDITED → GENERATING：用户确认覆盖后重新生成
- ERROR → READY：用户修改输入

#### 5.6.2 API 调用序列

```
用户点击"生成回复信"
  │
  ├─[阶段1] parse_review_points(review_text)
  │   └─ LLM: deepseek-chat, max_tokens=1000
  │   └─ 返回: 解析后的逐条意见列表
  │
  ├─[阶段2] 论文段落分析（客户端预处理）
  │   └─ 提取论文章节标题（正则匹配 # / Section / 第X节）
  │   └─ 构建章节索引 dict{section_name: start_char, end_char}
  │
  ├─[阶段3] generate_response_letter(paper, review, paper_title)
  │   └─ LLM: deepseek-chat, max_tokens=4000, temperature=0.5
  │   └─ 系统提示词包含：回复格式、学术语气、修改位置标注要求
  │   └─ 用户提示词：论文(截断8000字) + 审稿意见 + 解析结果
  │   └─ 要求 LLM 输出包含质量检查的完整回复信
  │
  ├─[阶段4] 后处理
  │   └─ 解析 LLM 输出中的对照表和质量检查部分
  │   └─ 组装 change_table (list of dicts)
  │   └─ 组装 quality_report (dict)
  │   └─ 返回统一结果
  │
  └─ 前端渲染三个标签页
```

#### 5.6.3 数据缓存策略

| 数据 | 存储位置 | 生命周期 | 清除条件 |
|------|---------|---------|---------|
| 论文原文 | gr.State | 当前会话 | 页面刷新 |
| 审稿意见原文 | gr.State | 当前会话 | 页面刷新 |
| 原始生成结果 | gr.State | 当前会话 | 重新生成覆盖 |
| 当前编辑内容 | gr.Textbox (DOM) | 当前会话 | 页面刷新/重新生成 |
| "已编辑"标记 | gr.State (bool) | 当前会话 | 重新生成 |
| 满意度评价结果 | 后端日志 | 无持久化 | 即时写入日志 |

#### 5.6.4 并发与竞态处理

| 场景 | 处理方式 |
|------|---------|
| 快速重复点击生成 | 按钮进入加载态后禁用，防抖300ms |
| 生成中切换标签页 | 允许，但显示骨架屏（内容尚未就绪） |
| 生成中修改输入 | 不允许（输入区在生成中锁定，Gradio 默认行为） |
| 复制时编辑 | 允许，复制的是当前最新内容（读DOM而非缓存） |
| 两次快速生成 | 第一次结果被第二次覆盖，无合并（符合预期：用户主动操作） |

---

### 5.7 全局错误处理矩阵

| 错误码 | 错误名称 | 触发条件 | 用户可见提示 | 用户可执行操作 | 系统恢复行为 | 阻断流程 | 埋点事件 |
|--------|---------|---------|------------|--------------|-------------|:--:|---------|
| E001 | 论文为空 | 点击生成时 paper_content 为空 | "请先上传论文或粘贴正文"（红色提示，论文卡片下方） | 上传文件或粘贴文本 | 无 | 是 | generate_error |
| E002 | 论文过短 | 点击生成时 len(paper)<100 | "论文内容太短（少于100字），请确保上传了完整的论文正文（至少包含摘要、引言、方法等核心章节）" | 补充内容 | 无 | 是 | generate_error |
| E003 | 审稿意见为空 | 点击生成时 review_text 为空 | "请先粘贴审稿意见。将期刊或会议的审稿意见邮件原文粘贴到右侧输入框" | 粘贴或上传 | 无 | 是 | generate_error |
| E004 | 论文过长警告 | len(paper)>50000 时黄色警告 | "论文字符数超过50000，生成时可能截断部分内容"（琥珀色提示条） | 缩短论文或忽略继续 | 截断至前8000字符送入LLM | 否 | - |
| E005 | API Key未配置 | .env 无 DEEPSEEK_API_KEY | "⚠️ API 密钥未配置。请检查 .env 文件中的 DEEPSEEK_API_KEY 设置" | 配置API Key后重试 | 无 | 是 | generate_error |
| E006 | API超时 | 生成超过60s无响应 | "⚠️ 生成超时。请尝试缩短论文长度后重试" | 点击重试按钮 | 重置进度指示器，释放请求 | 是 | generate_error |
| E007 | API返回错误 | DeepSeek 返回 4xx/5xx | "⚠️ 服务暂时不可用（{错误码}），请稍后重试" | 等待或重试 | 无 | 是 | generate_error |
| E008 | API连接失败 | 网络不可达 api.deepseek.com | "⚠️ 无法连接到 AI 服务，请检查网络连接后重试" | 检查网络，重试 | 无 | 是 | generate_error |
| E009 | 文件解析失败 | 上传损坏/加密/纯图片PDF | "[解析失败] 无法读取文件内容。请尝试：1) 手动复制论文正文粘贴到下方文本框 2) 将PDF另存为文本后重新上传" | 手动粘贴或重新保存文件 | 上传区恢复默认 | 否 | paper_upload |
| E010 | 文件格式不支持 | 上传 .jpg/.png/.xlsx 等 | "不支持的文件格式，请上传 PDF、Word、Markdown 或 TXT 文件"（抖动+红色提示） | 上传正确格式 | 上传区恢复默认 | 是 | paper_upload |
| E011 | 文件为空 | 上传0KB文件 | "[解析失败] 文件内容为空，请检查文件" | 重新上传 | 上传区恢复默认 | 是 | paper_upload |
| E012 | 审稿意见解析失败 | LLM返回乱码或极短内容 | "⚠️ 审稿意见解析异常，请检查意见格式后重试" | 检查输入，重试 | 无 | 是 | generate_error |
| E013 | 生成结果截断 | max_tokens不足，输出不完整 | "⚠️ 生成结果可能不完整。审稿意见较多（{N}条）时偶发截断。建议：1) 检查最后一条回复是否完整 2) 如不完整可缩短论文后重试" | 检查、缩短输入重试 | 无 | 否 | generate_success（含截断标记） |
| E014 | 质量检查数据缺失 | LLM未按预期格式输出质量检查 | 质量摘要卡片显示"质量检查暂不可用"（灰色文字） | 手动判断质量 | 降级显示 | 否 | - |
| E015 | 对照表行数不匹配 | 行数≠意见条数 | "⚠️ 对照表可能不完整（识别{N1}条但生成{N2}行），请检查" | 检查、重试 | 无 | 否 | - |
| E016 | 修改位置标注率低 | <80%行包含位置标注 | "⚠️ 仅{N}%的修改项包含位置标注，建议在回复信中补充具体修改位置" | 手动补充 | 无 | 否 | - |
| E017 | Clipboard API不可用 | 非HTTPS或旧浏览器 | 复制按钮置灰，文案"请手动 Ctrl+C 复制" | 手动选择+Ctrl+C | 按钮永久置灰 | 否 | - |
| E018 | 剪贴板写入失败 | 浏览器权限拒绝 | 复制按钮短暂变红"复制失败" | 重试或手动复制 | 2s后按钮恢复 | 否 | - |
| E019 | 论文超大(>8000字送LLM) | 自动截断 | 生成成功但提示"论文内容较长，前8000字已纳入生成上下文。建议分章节生成以获得更精确的结果" | 手动分章节生成 | 截断处理 | 否 | generate_success（含截断标记） |

---

### 5.8 性能规格

#### 5.8.1 时间预算

| 操作 | 目标耗时 | 最大可接受 | 测量方式 |
|------|:------:|:------:|---------|
| 页面首次加载 | < 2s | 3s | Lighthouse FCP |
| 文件解析（PDF/Word） | < 3s | 5s | 后端计时 |
| 审稿意见解析（LLM） | < 8s | 15s | 后端计时 |
| 回复信生成（LLM） | < 30s | 50s | 后端计时 |
| 对照表+质量检查（后处理） | < 5s | 10s | 后端计时 |
| 生成总耗时 | < 45s | 60s | 端到端计时 |
| 标签页切换 | < 200ms | 300ms | 前端渲染时间 |
| 复制按钮反馈 | 即时 | < 100ms | 前端交互时间 |
| 点击到视觉反馈 | < 100ms | 200ms | 前端交互时间 |

#### 5.8.2 超长文档处理策略

| 论文字符数 | 处理策略 |
|-----------|---------|
| 0 - 100 | 拦截，提示过短 |
| 100 - 8,000 | 全文送入 LLM |
| 8,000 - 50,000 | 截断至前 8,000 字符送入 LLM，生成后显示截断提示 |
| > 50,000 | 显示黄色警告，截断至前 8,000 字符，生成后显示截断提示 |

#### 5.8.3 API 超时与重试

- **超时设置**：60s（DeepSeek API）
- **重试次数**：1 次（首次超时后自动重试一次）
- **重试间隔**：0s（立即重试）
- **两次均超时**：显示 E006 错误，用户可手动点击重试
- **取消生成**：MVP 阶段不支持中止已发出的 LLM 请求

#### 5.8.4 前端渲染性能

- Markdown 渲染不阻塞主线程超过 50ms
- 对照表超过 20 行时考虑虚拟滚动（MVP 降级为普通渲染）
- CSS 动画仅使用 opacity 和 transform（GPU 加速属性）
- 图片/资源懒加载（如有）

---

### 5.9 国际化与本地化

#### 5.9.1 语言检测逻辑

| 场景 | 检测规则 | 回复语言 | 对照表语言 |
|------|---------|:--:|:--:|
| 审稿意见 100% 中文 | 中文字符占比 > 90% | 中文 | 中文 |
| 审稿意见 100% 英文 | 英文字符占比 > 90% | 英文 | 自动（中英对照预留） |
| 审稿意见中英混合 | 主要语言占比 > 70% 则使用该语言 | 主要语言 | 主要语言 |
| 无法判定 | 均不满足 → 默认中文 | 中文 | 中文 |

#### 5.9.2 UI 文案管理

MVP 阶段所有 UI 文案使用中文，英文文案作为预留字段在各功能的"文案清单"中标注。V2 根据用户地域分布决定是否做完整国际化。

---

## 六、数据埋点

| # | 触发条件 | 埋点事件 | 埋点参数 | 涉及终端 |
|:--:|---------|---------|---------|---------|
| 1 | 用户打开页面 | `page_view` | 页面来源（直接访问/分享链接）、时间戳、屏幕分辨率 | Web |
| 2 | 用户上传论文文件 | `paper_upload` | 文件格式（PDF/Word/MD/TXT）、文件大小（字符数）、解析成功/失败 | Web |
| 3 | 用户点击生成按钮 | `generate_click` | 论文字符数、审稿意见字符数、是否有上传文件 | Web |
| 4 | 生成开始 | `generate_start` | 时间戳（用于计算总耗时） | Web |
| 5 | 阶段1完成-意见解析 | `stage_parse_done` | 意见条数、审稿人数、耗时(ms) | Web |
| 6 | 阶段2完成-段落匹配 | `stage_match_done` | 匹配到的段落数、耗时(ms) | Web |
| 7 | 阶段3完成-回复生成 | `stage_generate_done` | 回复信字符数、耗时(ms)、是否截断 | Web |
| 8 | 阶段4完成-对照表 | `stage_table_done` | 对照表行数、质量检查通过率、耗时(ms) | Web |
| 9 | 生成成功 | `generate_success` | 总耗时(ms)、回复信字符数、对照表行数、质量检查通过率、是否截断 | Web |
| 10 | 生成失败 | `generate_error` | 错误类型（E001-E019）、错误信息、失败阶段 | Web |
| 11 | 用户切换标签页 | `tab_switch` | 切换到的标签（response/changes/quality） | Web |
| 12 | 用户点击复制按钮 | `copy_click` | 复制类型（回复信/对照表）、复制成功/失败 | Web |
| 13 | 用户编辑结果内容 | `result_edit` | 编辑类型（回复信/对照表）、编辑字符数 | Web |
| 14 | 用户点击满意度评价 | `feedback_submit` | 评价结果（useful/neutral/not_helpful）、可选文字反馈 | Web |
| 15 | 用户点击加载示例 | `example_load` | 触发来源（页面提示/主动点击） | Web |
| 16 | 确认对话框弹出 | `overwrite_dialog_show` | 触发场景（有编辑内容时重新生成） | Web |
| 17 | 用户选择覆盖/取消 | `overwrite_dialog_action` | 选择结果（confirm/cancel） | Web |

---

## 七、设计规范

### 7.1 设计原则

1. **学术但不沉闷**：暖石灰基调，避免深蓝色的学院派沉闷感，用材质和留白营造品质感。参考 Linear 的极简克制和 Stripe 的精致感。
2. **结构化而非对话式**：ReviewResponse 是工具而非聊天机器人。使用卡片、表格、标签页等结构化组件，而非对话气泡。
3. **渐进披露**：默认最简，按需展开。新用户看到的是两个输入框和一个按钮，生成完成后才展示完整输出区。
4. **反馈即信任**：每个操作都有即时、明确、温和的反馈。不静默失败，不使用技术性错误提示。
5. **内容优于装饰**：14-16px 正文字号、1.6-1.7 行高、充足留白，确保学术长文本的可读性。

### 7.2 配色系统

#### 7.2.1 主色板（暖石色系）

| Token | 色值 | 用途 |
|-------|------|------|
| `stone-950` | `#0c0a09` | 最深层，仅用于文字强调 |
| `stone-900` | `#1c1917` | 主按钮底色、主标题 |
| `stone-800` | `#292524` | 主按钮悬停、正文文字 |
| `stone-700` | `#44403c` | 次要标题 |
| `stone-600` | `#57534e` | 次按钮文字 |
| `stone-500` | `#78716c` | 次要文字、标签未选中 |
| `stone-400` | `#a8a29e` | placeholder文字、辅助文字 |
| `stone-300` | `#d6d3d1` | 上传区虚线边框 |
| `stone-200` | `#e7e5e4` | 边框、分隔线 |
| `stone-100` | `#f5f5f0` | 输入框悬停背景 |
| `stone-50` | `#fafaf8` | 页面背景、输入框背景 |

#### 7.2.2 功能色

| Token | 色值 | 用途 |
|-------|------|------|
| `success` | `#16a34a` | ✓ 标记、通过状态、复制成功边框 |
| `success-bg` | `#f0fdf4` | 复制成功背景、质量卡片全通过背景 |
| `warning` | `#d97706` | ⚠ 标记、警告文字、字符超限 |
| `warning-bg` | `#fffbeb` | 质量卡片有警告背景、警告提示条 |
| `warning-badge` | `#fef3c7` | "已编辑"徽章背景 |
| `warning-badge-text` | `#92400e` | "已编辑"徽章文字 |
| `error` | `#dc2626` | ✗ 标记、错误边框、错误文字 |
| `error-bg` | `#fef2f2` | 错误提示背景 |
| `info` | `#2563eb` | 预留（信息提示） |
| `info-bg` | `#eff6ff` | 预留（信息提示背景） |

#### 7.2.3 对比度合规

| 组合 | 对比度 | WCAG级别 |
|------|:--:|:--:|
| #1c1917 文字 on #ffffff 卡片 | 15.2:1 | AAA |
| #1c1917 文字 on #fafaf8 背景 | 14.1:1 | AAA |
| #ffffff 文字 on #1c1917 按钮 | 15.2:1 | AAA |
| #78716c 文字 on #ffffff 卡片 | 5.4:1 | AA |
| #a8a29e placeholder on #fafaf8 | 2.9:1 | 不达标（placeholder例外） |
| #16a34a 图标 on #ffffff | 3.4:1 | AA（图标16px+例外） |
| #d97706 图标 on #ffffff | 3.1:1 | AA（图标16px+例外） |

### 7.3 字体系统

#### 7.3.1 字体族

```css
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
             "Helvetica Neue", Arial, "Noto Sans SC", "PingFang SC",
             "Microsoft YaHei", sans-serif;
```

- 英文优先系统字体（-apple-system / Segoe UI）
- 中文 fallback：Noto Sans SC → PingFang SC → Microsoft YaHei
- 等宽字体（代码块）：`"SF Mono", "Fira Code", "Consolas", monospace`

#### 7.3.2 字号-行高-字重映射表

| 级别 | 字号 | 行高 | 字重 | 字间距 | 用途 |
|:--:|:--:|:--:|:--:|:--:|------|
| T1 | 32px | 1.25 | 700 | -0.5px | 页面标题（Logo） |
| T2 | 20px | 1.3 | 600 | -0.3px | 大区标题 |
| T3 | 16px | 1.5 | 600 | 0 | 卡片标题、对话框标题 |
| T4 | 15px | 1.6 | 400 | 0 | 正文、输入框文字 |
| T5 | 14px | 1.7 | 400 | 0 | 输出区正文、按钮文字 |
| T6 | 13px | 1.5 | 600 | 0.8px | 区域标签（大写） |
| T7 | 13px | 1.5 | 400 | 0 | 进度步骤文字 |
| T8 | 12px | 1.4 | 400 | 0 | 辅助文字、tooltip、徽章 |
| T9 | 11px | 1.3 | 400 | 0 | 极小文字（预留） |

### 7.4 间距系统

基于 4px 线性比例：

| Token | 值 | 用途 |
|-------|:--:|------|
| `space-xs` | 4px | 图标与文字间距 |
| `space-sm` | 8px | 标签与字段间、紧密元素 |
| `space-md` | 12px | 列表项间距 |
| `space-lg` | 16px | 按钮间、标签内容间距 |
| `space-xl` | 20px | 卡片内 padding |
| `space-2xl` | 24px | 卡片 padding、区域内间距 |
| `space-3xl` | 32px | 卡片间间距 |
| `space-4xl` | 40px | 区域间间距 |
| `space-5xl` | 48px | Header 下方间距 |
| `space-6xl` | 64px | 最大留白 |

### 7.5 圆角系统

| Token | 值 | 用途 |
|-------|:--:|------|
| `radius-sm` | 6px | 徽章、tooltip |
| `radius-md` | 10px | 输入框、按钮、上传区 |
| `radius-lg` | 12px | 输出区、标签面板 |
| `radius-xl` | 16px | 卡片、对话框、面板 |

### 7.6 阴影系统

| Token | 值 | 用途 |
|-------|-----|------|
| `shadow-card` | `0 1px 3px rgba(0,0,0,0.04), 0 4px 12px rgba(0,0,0,0.04)` | 卡片默认 |
| `shadow-card-hover` | `0 1px 3px rgba(0,0,0,0.06), 0 6px 20px rgba(0,0,0,0.06)` | 卡片悬停 |
| `shadow-button` | `0 1px 3px rgba(28,25,23,0.12)` | 主按钮默认 |
| `shadow-button-hover` | `0 4px 14px rgba(28,25,23,0.18)` | 主按钮悬停 |
| `shadow-dialog` | `0 0 0 1px rgba(0,0,0,0.06), 0 16px 48px rgba(0,0,0,0.12)` | 对话框 |
| `shadow-tooltip` | `0 2px 8px rgba(0,0,0,0.08)` | Tooltip |

### 7.7 动效系统

#### 7.7.1 时长 Token

| Token | 值 | 用途 |
|-------|:--:|------|
| `duration-instant` | 0ms | 即时状态切换（如按钮禁用/激活） |
| `duration-micro` | 100ms | 按下态、tooltip消失 |
| `duration-quick` | 150ms | 边框色过渡、标签切换、徽章出现 |
| `duration-standard` | 200ms | 卡片悬停、按钮悬停、对话框遮罩 |
| `duration-slow` | 300ms | 内容淡入、对话框弹入、抖动动画 |
| `duration-reveal` | 500ms | 页面区域展现、滚动动画 |

#### 7.7.2 缓动函数

| Token | 值 | 用途 |
|-------|-----|------|
| `ease-default` | `cubic-bezier(0.4, 0, 0.2, 1)` | 大多数过渡（标准 Material Design 缓动） |
| `ease-bounce` | `cubic-bezier(0.34, 1.56, 0.64, 1)` | 弹跳效果（checkmark出现、对话框进入、脉冲） |
| `ease-emphasis` | `cubic-bezier(0.4, 0, 0, 1)` | 强调入场（内容区域淡入） |

#### 7.7.3 动效场景清单

| # | 场景 | 属性 | 时长 | 缓动 |
|:--:|------|------|:--:|------|
| 1 | 按钮悬停上浮 | transform:translateY(-1px) | 200ms | ease-default |
| 2 | 按钮悬停阴影加深 | box-shadow | 200ms | ease-default |
| 3 | 卡片悬停 | box-shadow | 200ms | ease-default |
| 4 | 标签下划线滑动 | left/width（JS计算） | 200ms | ease-default |
| 5 | 内容区标签切换 | opacity 0→1 | 150ms | ease-default |
| 6 | 进度条 checkmark 出现 | scale 0→1.2→1 | 300ms | ease-bounce |
| 7 | 复制按钮成功反馈 | background-color, icon | 150ms | ease-default |
| 8 | 复制成功弹跳 | scale 1→1.05→1 | 200ms | ease-bounce |
| 9 | Toast 入场 | opacity 0→1, translateY(8px→0) | 300ms | ease-emphasis |
| 10 | Toast 退场 | opacity 1→0, translateY(0→-4px) | 200ms | ease-default |
| 11 | 对话框遮罩 | opacity 0→1 | 200ms | ease-default |
| 12 | 对话框卡片 | scale 0.95→1.02→1 | 300ms | ease-bounce |
| 13 | 文件拖入反馈 | border-color, background-color, scale | 200ms | ease-default |
| 14 | 错误抖动 | translateX(0→-4px→4px→-4px→0) | 300ms | ease-default |
| 15 | 徽章出现 | opacity 0→1, scale 0.8→1 | 200ms | ease-bounce |
| 16 | 骨架屏脉冲 | opacity 0.4→0.6→0.4 循环 | 1.5s | ease-default(infinite) |
| 17 | 字符计数淡入 | opacity 0→1 | 150ms | ease-default |
| 18 | 质量卡片出现 | opacity 0→1, translateY(12px→0) | 300ms | ease-emphasis |

### 7.8 屏幕线框图描述

#### S0：初始空状态

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                      ReviewResponse           (32px/700)    │
│          粘贴论文与审稿意见，AI 自动生成逐条回复草稿 (16px)  │
│                                                             │
│  ┌─────────────────────┐  ┌─────────────────────┐           │
│  │ 论文原文 (13px/600)  │  │ 审稿意见 (13px/600)  │           │
│  │ ┌─────────────────┐ │  │ ┌─────────────────┐ │           │
│  │ │  📤 拖拽文件...  │ │  │ │  📤 拖拽文件...  │ │           │
│  │ │  PDF/Word/MD/TXT│ │  │ │  PDF/Word/MD/TXT│ │           │
│  │ └─────────────────┘ │  │ └─────────────────┘ │           │
│  │ ┌─────────────────┐ │  │ ┌─────────────────┐ │           │
│  │ │ 或直接粘贴正文   │ │  │ │ 或直接粘贴意见   │ │           │
│  │ │                 │ │  │ │                 │ │           │
│  │ │ (placeholder)   │ │  │ │ (placeholder)   │ │           │
│  │ └─────────────────┘ │  │ └─────────────────┘ │           │
│  └─────────────────────┘  └─────────────────────┘           │
│                                                             │
│    [加载示例数据]              [生成回复信](禁用40%)         │
│                                                             │
│  ───────────────────────────────────────────────────────    │
│                                                             │
│    回复信 Response Letter              审稿意见解析          │
│    ┌──────────────────────┐  ┌──────────────────────┐       │
│    │ 点击「生成回复信」后  │  │ 审稿意见将自动拆分为  │       │
│    │ AI将在此生成完整回复  │  │ 逐条编号，按审稿人分组 │       │
│    └──────────────────────┘  └──────────────────────┘       │
│                                                             │
│  ════════════════ Footer (特色说明) ═══════════════════    │
└─────────────────────────────────────────────────────────────┘
```

#### S1：输入就绪（已加载示例或用户填入内容）

与 S0 布局相同，差异点：
- 论文输入框有内容，右下角显示字符计数"3,245 字符"
- 审稿意见输入框有内容，右下角显示"1,082 字符"
- "生成回复信"按钮完全激活（暖黑底色 #1c1917）
- 如通过文件上传：输入框上方显示文件卡片（PDF 徽章 + 文件名 + 字符数 + ×）

#### S2：生成中（进度指示器展示）

输入区保持不变（锁定不可编辑），输出区替换为进度指示器：

```
  ───────────────────────────────────────────────────────
  
     ● ─── ● ─── ◉ ─── ○
     ✓      ✓     ⚡     
   解析    匹配   生成    整理
   完成    完成   中...   等待
  
   正在生成回复草稿，预计还需 10-25 秒...
  
  ┌──────────────────────────────────────────────────────┐
  │  ████████████░░░░░░░░░░  (骨架屏：3条灰色脉冲条)      │
  │  ██████████░░░░░░░░░░░░                             │
  │  ████████████░░░░░░░░░░                             │
  └──────────────────────────────────────────────────────┘
```

#### S3：结果展示-回复信标签

```
  ───────────────────────────────────────────────────────
  
  [回复信 Response Letter] [修改对照表 Change Table] [质量报告 Quality]
  ─────────────────────────
  
  ┌──────────────────────────────────────────────────────┐
  │ 尊敬的编辑和审稿人：                                  │
  │                                                      │
  │ 感谢您们在百忙之中审阅我们的稿件...                     │
  │                                                      │
  │ ### Reviewer 1                                       │
  │                                                      │
  │ **Comment 1:** The literature review...               │
  │ **Response 1:** 感谢审稿人的宝贵意见...                │
  │ **修改位置:** 第2节第3-5段                            │
  │                                                      │
  │ （更多回复...）                                       │
  │                                                      │
  │ ---                                                  │
  │ ## 质量摘要                                          │
  │ | 检查维度 | 结果 | 说明 |                            │
  │ |---------|:--:|------|                             │
  │ | 位置标注 | 7/8 | 第3条缺少章节标注 |                 │
  │ | 语气适宜 | 8/8 | 全部通过 |                         │
  │ | 内容完整 | 6/8 | 第5、8条未完全回应 |                 │
  └──────────────────────────────────────────────────────┘
  
                               [📋 复制回复信]
                               
  ───────────────────────────────────────────────────────
  
     这次生成结果对你有帮助吗？
     [👍 有用]  [😐 一般]  [👎 无帮助]
```

#### S4：结果展示-修改对照表标签

标签页切换到"修改对照表"，下划线滑动到该标签下方。内容区渲染五列 Markdown 表格。

#### S5：结果展示-质量报告标签

标签页切换到"质量报告"，内容区展示：
- 总体质量摘要（通过率）
- 三维度各自的详细结果
- 每条意见的检查详情（可展开）

#### S10：编辑模式

与 S3 相同，差异点：
- 输出区右上角出现"已编辑"琥珀徽章
- 用户可直接在文本框中修改
- 再次点击"生成回复信"会弹出确认对话框

```
  ┌──────────────────────────────────────┐
  │  ⚠️ 覆盖编辑内容                      │
  │                                      │
  │  再次生成将覆盖您在回复信和对照表中    │
  │  所做的编辑内容，是否继续？            │
  │                                      │
  │         [取消]  [继续生成]            │
  └──────────────────────────────────────┘
```

### 7.9 组件规格详表

（已在 5.5 组件状态矩阵中详细列出，此处不再重复。7.9 作为视觉设计token的补充，5.5 作为交互状态的定义。）

### 7.10 竞品交互参考手册

| 交互模式 | 参考产品 | 参考点 | 我们采用 | 差异化原因 |
|---------|---------|--------|---------|-----------|
| 文件拖拽上传 | Stripe Elements | 三态切换（默认虚线/悬停实线/已上传卡片） | 全盘采用 + 增加格式徽章 | 学术场景多格式上传 |
| 复制按钮反馈 | Stripe API Docs | 1.5s ✓ "已复制" 反馈 | 全盘采用 | 成熟交互范式的用户预期 |
| 进度指示器 | Vercel Deploy | 多阶段步骤条，每阶段独立状态 | 采用 + 改为4阶段 + 每阶段文字描述 | 生成流程特有的4步结构 |
| 标签页切换 | Notion DB Views | 下划线滑动动画，内容交叉淡入 | 全盘采用 | 信息架构类似（多个视图看同一数据） |
| 内联编辑 | Notion Block | 点击进入编辑态，"已编辑"标记 | 简化：输出区默认即可编辑 | Gradio 限制，不做点击切换 |
| 质量徽章 | Grammarly Tone | 行级标记 + 底部整体评分卡片 | 简化：二值标记(✓/⚠) + 三维度摘要 | MVP 降级，V2 可扩展为分数 |
| 满意度评价 | ChatGPT Feedback | 三按钮互斥 + 展开文本 + 确认态 | 全盘采用 | 成熟交互范式的用户预期 |
| 空状态引导 | Linear Empty States | 简洁图标 + 一句话说明 + CTA 按钮 | 采用：placeholder + "加载示例数据"按钮 | 降低首次使用门槛 |
| 加载骨架屏 | Notion / Linear | 灰色脉冲条模拟内容布局 | 采用：生成中输出区显示骨架屏 | 降低等待焦虑 |

---

## 八、技术约束与设计约束

### 8.1 Gradio 框架约束

| 约束 | 影响 | 应对 |
|------|------|------|
| 无客户端路由 | 无法实现 URL 分享带状态（如 `#changes` tab） | MVP 接受，V2 考虑迁移到 Next.js |
| 组件事件有限 | gr.Button 不支持自定义 JS 事件（仅 click） | 复制、评价等交互通过 gr.HTML 注入 JS |
| CSS 通过字符串注入 | 无法使用 CSS Modules 或 styled-components | 使用 BEM-like 命名（如 `.app-header .logo`） |
| 无响应式断点控制 | 布局在移动端可能错位 | MVP 仅支持桌面端 >= 960px |
| 不支持流式输出 | 生成结果需等待完整返回 | 通过进度指示器补偿等待体感 |

### 8.2 Markdown 渲染限制

| 限制 | 影响 | 应对 |
|------|------|------|
| 表格不支持合并单元格 | 对照表可能较宽 | 控制每列宽度（≤30字/≤60字） |
| 表格内不支持复杂格式 | 修改位置列的章节引用不能加粗或变色 | 使用纯文本 + emoji 方案 |
| 无代码语法高亮 | 论文中的代码块显示为纯文本 | 可接受（论文代码块非核心） |

### 8.3 无数据库约束

| 约束 | 影响 | 应对 |
|------|------|------|
| 页面刷新丢失所有数据 | 用户误刷新需重新输入和生成 | 生成完成后提示用户尽快复制 |
| 无法查看历史生成 | 用户无法对比两次生成结果 | V2 通过 localStorage 或后端数据库实现 |
| 满意度评价无法持久化 | 仅写入后端日志，无结构化存储 | MVP 可接受 |

### 8.4 API 限制

| 约束 | 值 | 影响 | 应对 |
|------|-----|------|------|
| max_tokens | 4000 | 长论文+多意见时可能截断 | 论文截断至8000字符，提示用户 |
| timeout | 60s | 超过60s自动断开 | 自动重试1次，失败后提示用户缩短输入 |
| 并发限制 | DeepSeek 免费配额 | 高并发时可能排队 | MVP 阶段用户量小，可接受 |

### 8.5 浏览器兼容性

| 浏览器 | 最低版本 | 说明 |
|--------|:--:|------|
| Chrome | 90+ | 主要开发和测试平台 |
| Firefox | 90+ | 次要支持 |
| Safari | 15+ | 次要支持 |
| Edge | 90+ | 基于 Chromium，应与 Chrome 表现一致 |
| IE | 不支持 | - |

### 8.6 性能要求

| 指标 | 目标 | 测量工具 |
|------|:--:|------|
| First Contentful Paint | < 1.5s | Lighthouse |
| Time to Interactive | < 3s | Lighthouse |
| 交互响应时间 | < 100ms | 手动计时 |
| 生成总耗时 | < 45s | 后端计时 |
| CSS 动画帧率 | 60fps | Chrome DevTools |

---

## 九、验收标准总表

### 9.1 功能验收清单

| # | 功能 | 验收条件 | 验证方法 | 关联屏幕 | 关联埋点 |
|:--:|------|---------|---------|:--:|:--:|
| F1-1 | 论文上传 | 拖拽PDF→解析→文本框填入→字符计数→格式徽章 | 手动拖拽测试 | S0→S1 | paper_upload |
| F1-2 | 论文上传-格式校验 | 上传.jpg→抖动+错误提示 | 手动上传错误格式 | S0 | paper_upload |
| F1-3 | 论文上传-解析失败 | 上传损坏PDF→错误提示+手动粘贴引导 | 手动上传损坏文件 | S0 | paper_upload |
| F1-4 | 论文校验 | 空→拦截 / <100字→拦截 / >50000→警告不阻断 | 边界值测试 | S1 | generate_error |
| F2-1 | 审稿意见输入 | 粘贴文本→字符计数 | 手动测试 | S1 | - |
| F2-2 | 审稿意见解析 | 点击生成→第1阶段解析→正确分组+编号+分类 | 生成后验证 | S2 | stage_parse_done |
| F2-3 | 审稿意见校验 | 为空→拦截 | 边界值测试 | S1 | generate_error |
| F3-1 | 回复信生成 | 包含致谢开头+逐条回复+结尾致谢 | 生成后检查 | S3 | stage_generate_done |
| F3-2 | 回复信格式 | Markdown正确渲染（标题/列表/加粗） | 目视检查 | S3 | - |
| F3-3 | 回复语言匹配 | 中文意见→中文回复 / 英文意见→英文回复 | 中英文各测一次 | S3 | - |
| F3-4 | 回复质量 | 每条回复包含感谢+修改说明+位置标注 | 抽样检查 | S3 | - |
| F4-1 | 对照表生成 | 五列表格，行数=意见数 | 生成后验证 | S4 | stage_table_done |
| F4-2 | 对照表内容 | ≥80%行含章节标注 / 意见要点≤30字 / 摘要≤60字 | 抽样检查 | S4 | - |
| F4-3 | 状态标记 | ✓或⚠正确标记每行 | 生成后验证 | S4 | - |
| F5-1 | 复制-回复信 | 点击→剪贴板写入→按钮"✓已复制"1.5s | 手动测试 | S3 | copy_click |
| F5-2 | 复制-对照表 | 同上逻辑 | 手动测试 | S4 | copy_click |
| F5-3 | 复制-降级 | 无Clipboard API→按钮置灰+手动提示 | 模拟旧浏览器 | S3/S4 | - |
| F6-1 | 加载示例 | 点击→两输入框同时填入→生成按钮激活 | 手动测试 | S0→S1 | example_load |
| F6-2 | 覆盖行为 | 已有内容时加载示例→直接覆盖（无确认框） | 手动测试 | S1 | - |
| F7-1 | 质量检查-三项维度 | 位置标注/语气适宜/内容完整结果正确 | 生成后验证 | S3/S4 | - |
| F7-2 | 质量摘要卡片 | 全部通过→绿色 / 有警告→琥珀色 | 生成后验证 | S3 | - |
| F7-3 | Tooltip | ⚠悬停→显示未通过检查项 | 鼠标悬停测试 | S4 | - |
| F8-1 | 结果编辑 | 修改内容→"已编辑"徽章出现 | 手动编辑 | S6 | result_edit |
| F8-2 | 覆盖确认 | 已编辑+重新生成→对话框→取消/继续 | 手动测试 | S6 | overwrite_dialog_show |
| F8-3 | 编辑恢复 | 恢复原始内容→徽章消失 | 手动测试 | S6→S3 | - |

### 9.2 交互验收清单

| # | 交互模式 | 验收条件 | 验证方法 |
|:--:|------|---------|---------|
| I1 | 生成进度 | 4阶段依次推进：pending→in-progress→done，每阶段状态正确 | 生成过程中观察 |
| I2 | 标签页切换 | 点击→下划线滑动200ms→内容交叉淡入150ms | 切换标签页 |
| I3 | 一键复制 | 点击→按钮变绿"✓已复制"→1.5s恢复 | 点击复制按钮 |
| I4 | 文件拖拽 | 拖入→边框变色+背景变色+scale1.01→释放→解析→文件卡片 | 拖拽文件 |
| I5 | 满意度反馈 | 点击→三个按钮置灰+选中高亮+👎展开文本框 | 点击评价按钮 |
| I6 | 覆盖确认对话框 | 弹入动画→取消关闭→确认后覆盖+重新生成 | 编辑后重新生成 |
| I7 | 骨架屏 | 生成中输出区显示脉冲骨架屏→完成后淡入内容 | 生成过程中观察 |
| I8 | 字符计数 | 空→隐藏 / <100→红色 / 100-50000→灰色 / >50000→琥珀色 | 输入不同长度文本 |

### 9.3 设计验收清单

| # | 检查项 | 标准 | 验证方法 |
|:--:|------|------|---------|
| D1 | 颜色一致性 | 使用设计Token而非硬编码色值（#1c1917按钮, #fafaf8背景, #e7e5e4边框） | 审查CSS和主题配置 |
| D2 | 字体层级 | T1-T8正确应用于对应元素（标题32px/700，正文15px/400，标签13px/600） | 目视+DevTools检查 |
| D3 | 间距一致性 | 区域间40px，卡片内24px，紧密元素16px，标签字段8px | DevTools测量 |
| D4 | 圆角规范 | 卡片16px，输入框10px，徽章6px | DevTools检查 |
| D5 | 阴影规范 | 卡片双层阴影，按钮单层阴影，悬停加深 | DevTools检查 |
| D6 | 动效时长 | 过渡150-300ms，不超过500ms | DevTools Performance面板 |
| D7 | 对比度 | 所有文字-背景组合 ≥ AA级（placeholder除外） | 对比度检查工具 |
| D8 | 焦点可见 | 所有可交互元素有可见的 focus ring | Tab键遍历页面 |

### 9.4 错误处理验收清单

验证每个错误码（E001-E019）的：触发条件正确性、用户可见文案准确性、用户可执行操作的可用性、系统恢复行为的正确性。

（详见 5.7 全局错误处理矩阵，共19个错误场景，逐一验证）

### 9.5 可访问性验收清单

| # | 检查项 | 标准 |
|:--:|------|------|
| A1 | 键盘导航 | Tab/Shift+Tab 可按逻辑顺序遍历所有可交互元素 |
| A2 | 焦点可见 | 所有可交互元素有可见的 focus ring |
| A3 | 按钮 aria-label | 所有无文字按钮(icon-only)有 aria-label |
| A4 | 动态内容播报 | 生成完成/错误/复制成功有 aria-live 播报 |
| A5 | 标题层级 | h1(Logo)→h2(区域标题)→h3(卡片标题)→h4(内容标题) |
| A6 | 表格语义化 | th scope="col", 正确的 caption |
| A7 | 对比度 | 所有文字-背景 ≥ AA（placeholder除外） |
| A8 | 对话框焦点管理 | 打开对话框→焦点自动移入 / 关闭→焦点回到触发元素 |

---

## 附录

### 相关文档

1. 用户调研分析报告：[用户调研分析报告.md](用户调研分析报告.md)
2. 用户调研问卷：[用户调研问卷_审稿意见回复痛点.md](用户调研问卷_审稿意见回复痛点.md)
3. 竞品分析：[竞品分析_填充版.md](竞品分析_填充版.md)
4. 学术写作全流程痛点图谱：[学术写作全流程痛点图谱.md](学术写作全流程痛点图谱.md)
5. 竞品清单：[竞品清单.md](竞品清单.md)
6. 用户画像：见用户调研分析报告第七章和用户调研填充版第五节
7. 当前原型：[app.py](app.py)
8. 学术搜索工具模块（复用）：[src/tools/academic_search.py](src/tools/academic_search.py)
9. 后端代码：[src/review_response.py](src/review_response.py)
