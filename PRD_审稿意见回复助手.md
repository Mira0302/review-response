# 产品需求文档：审稿意见回复助手 ReviewResponse

> 版本：V1.0 MVP · 创建时间：2026年5月 · 需求负责人：YN

---

## 一、需求介绍

| 字段 | 内容 |
|------|------|
| 所属业务线 | AI 学术写作工具 |
| 需求负责人 | YN |
| 需求介绍 | 基于大语言模型，为有投稿经验的研究生和青年研究者提供审稿意见辅助回复工具。用户上传论文原文并粘贴审稿意见后，系统自动解析意见条目、生成逐条回复草稿并提供修改对照表。MVP 阶段为 Web 网页形态，核心验证三个假设：用户是否愿意使用专用工具替代 ChatGPT 做审稿回复、修改对照表功能是否足够差异化、愿意付费的用户比例是否与调研一致。 |

---

## 二、需求背景

### 2.1 需求价值

全球每年发表论文约 500 万篇，投稿量约为发表量的 2 到 3 倍，意味着每年有超过 1000 万次"收到审稿意见然后撰写回复信"的场景发生。非英语母语研究者占全球科研人员的百分之六十以上，中国研究生超 300 万。

用户调研（n=33）显示：百分之九十四的用户已经在用 ChatGPT 做审稿回复，但百分之六十七的用户认为其输出"一眼假，不敢用"。问题不在于 AI 写不出文字，而在于通用大模型没有论文上下文、不懂审稿回复的专业格式和语气规范、用户自己也无法判断生成结果的好坏。百分之七十九的用户明确表示需要一款专用工具，百分之七十愿意付费。

做审稿回复这个方向，不是跟 ChatGPT 比谁的文字生成能力更强，而是在它的盲区做深度：论文上下文感知、修改位置自动标注、学术语气内置保证、回复质量提示。

### 2.2 目标用户

**核心用户**：有第一作者或通讯作者投稿经历的硕士研究生和博士研究生，刚经历或正在经历第一次到第三次修回流程。学术写作经验不足，审稿回复信写作规范尚未建立，当前依赖 ChatGPT 加师兄师姐模板的手工拼接方案。

**次要用户**：有稳定投稿经验的博士后和青年教师，已有成熟的审稿回复习惯，对效率提升有需求但痛点不如核心用户强烈。

**非目标用户**：尚未投过稿的本科生，无修回经历，不在早期推广范围。

### 2.3 典型使用场景

**场景一：第一次投稿的硕士收到 Minor Revision**

研二学生小张投了一篇中文核心期刊，收到三周后返回的 Minor Revision，审稿意见共五条。小张打开 ChatGPT 把意见粘贴进去让它生成回复，但生成的文字"感觉很假"，也不知道回复信的格式对不对。她找到学姐要了一份过往的回复信模板，对照着改写，前前后后花了一个下午。她最担心的是"回复写得不够好，审稿人直接拒了"。

在这个场景中，ReviewResponse 的价值是：她只需上传论文和粘贴审稿意见，系统自动拆解意见、生成符合学术规范的逐条回复草稿，并告诉她每条回复的语气是否合适、修改位置是否已标注。她不需要自己会写 prompt，不需要找学姐要模板，也不需要反复纠结"写得对不对"。

**场景二：被导师反复退回修改的博士**

博三学生小李投了一篇 SCI 期刊，收到 Major Revision 共十二条审稿意见。他花了三天改完论文，又花了一天写回复信。导师看了回复信草稿不满意，让他逐条修改措辞和补充理由。他反复改了五六版，每次都要重新标注哪些修改对应论文的哪个位置。最崩溃的是改到后期已经搞不清 v3 和 v5 的区别了。

在这个场景中，ReviewResponse 的价值是：系统生成回复草稿的同时自动输出修改对照表，左栏是审稿意见要点，右栏标注论文中对应的修改段落位置。导师或合作者可以直接在表上审批，无需对着论文原文查找。

**场景三：英文期刊的回复焦虑**

研二学生小王第一次投英文期刊，审稿意见全是英文。虽然大致看得懂，但写英文回复信时完全拿不准语气——怕太随意显得不专业，又怕太正式显得僵硬。她用 DeepL 辅助写作，但翻译出来的表达有时很生硬，自己又判断不了。

在这个场景中，ReviewResponse 的价值是：系统内置学术英文回复的语气规范，生成中英对照版回复信，用户可以先确认中文内容准确无误，再查看英文表达是否地道。系统还可标注出可能导致语气问题的措辞，提醒用户斟酌。

### 2.4 竞品分析

#### 2.4.1 分析结论

1. 审稿回复辅助是一个品类空白。目前没有发现任何产品专门做"审稿意见到逐条回复"这件事。最接近的 Paperpal 在审稿回复环节仅做格式检查层面的简单校验，不生成回复内容。
2. 用户当前默认方案是 ChatGPT（使用率百分之九十四），但满意度低。两个核心缺口：一是 ChatGPT 没有论文原文上下文，用户需要在两个窗口间反复对照；二是 ChatGPT 不了解审稿回复信的格式规范和语气要求，用户需要自己设计 prompt 作文本引导。
3. 不跟 ChatGPT 比生成能力，比它做不到的事：论文段落自动匹配、学术语气内置、修改位置标注、回复质量提示。
4. 进入门槛不高（MVP 核心技术是 LLM prompt 加论文段落匹配），长期壁垒在于持续积累的审稿回复语料和用户行为数据。

#### 2.4.2 竞品方案

**方案概述**：目前审稿回复场景不存在专用竞品。以下是用户调研中反映最常用的替代方案。

**用户当前方案：ChatGPT 加手工拼接**

用户将审稿意见粘贴进 ChatGPT，通过手动设计的 prompt 引导生成回复。然后自己整理格式，对照论文原文逐条检查修改位置是否正确标注，最后参考师兄师姐的过往回复信模板调整语气和措辞。整个流程涉及 ChatGPT、论文原文（Word 或 Overleaf）、过往模板、翻译工具等多个工具的切换，核心痛点是自己对照的机械劳动和自己判断质量的认知压力。

**最接近的间接竞品：Paperpal**

Paperpal 定位投稿前全流程学术写作平台，覆盖润色、翻译、投稿格式检查、语言编辑等功能。审稿回复仅为其二十余个功能中的一个小模块，功能逻辑是检查用户已写好的回复信是否满足目标期刊的格式要求（如是否包含致谢段落、文件清单是否齐全），不涉及回复内容的生成或审稿意见的解析。交互上以 Word 插件侧边栏的形式嵌入编辑器，修改建议的展示方式类似 Word 修订模式。

**竞品对比要点**：Paperpal 做的是"帮你检查你已经写完的回复信格式对不对"，ReviewResponse 做的是"帮你写出一封回复信"。两者在审稿回复场景中不存在直接竞争。

---

## 三、需求目标

### 3.1 需求目标

1. 验证用户是否愿意从 ChatGPT 迁移到专用工具完成审稿回复任务
2. 验证修改对照表功能是否构成足够的差异化壁垒
3. 积累首批用户的行为数据和反馈，为 V2 版本的功能决策提供依据
4. 跑通"上传到生成到审核"的完整闭环，建立可迭代的产品基线

### 3.2 衡量指标

| 指标 | 计算方式 | 目标值 |
|------|---------|--------|
| 用户满意度 | 生成结果后下发满意度评价（有用、一般、无帮助） | 有用比例不低于百分之六十 |
| 生成完成率 | 成功生成回复信的次数除以点击生成按钮的次数 | 不低于百分之九十 |
| 用户留存 | 一周内使用过两次及以上的用户数除以总用户数 | 不低于百分之三十 |
| 分享转化 | 通过分享链接访问的新用户数占总访问用户数的比例 | 不低于百分之二十 |
| 平均使用时长 | 从打开页面到关闭页面的平均停留时间 | 不低于三分钟 |

---

## 四、需求范围

**MVP 必做（P0）**：

1. 论文原文上传。支持 PDF、Word、Markdown、纯文本四种格式，自动解析提取全文内容
2. 审稿意见输入。支持直接粘贴和文件上传两种方式，自动解析审稿意见并拆分为逐条条目
3. 回复信生成。基于论文原文和审稿意见，生成包含致谢开头、逐条回复、结尾的完整回复信。内置学术回复信的语气和格式规范
4. 修改对照表生成。在回复信之外单独输出修改对照表，逐条标注审稿意见要点与论文中的对应修改位置
5. 一键复制。支持将回复信全文或修改对照表一键复制到剪贴板

**MVP 次要做（P1）**：

6. 回复质量提示。每条生成的回复附带简洁的语气和完整性提示，标明潜在问题
7. 结果编辑。用户可在输出区直接编辑回复信和对照表的文字内容
8. 中英对照模式。当审稿意见为英文时，提供中英对照版回复信，中文确认内容、英文查看措辞

**不做（P2及以后）**：

- 用户注册登录系统
- 历史生成记录保存
- 回复模板库
- 导师在线审批批注
- 一键导出 Word 或 PDF
- 移动端适配
- 语法检查与润色
- 期刊格式适配

---

## 五、需求详情

### 5.1 页面布局总览

页面从上到下分为五个区域：

```
┌──────────────────────────────────────────────┐
│  ① Header                                    │
│  ReviewResponse ｜ 副标题                      │
├──────────────────────────────────────────────┤
│  ② 输入区（双卡片并排）                         │
│  ┌──────────────┐  ┌──────────────┐          │
│  │  论文原文     │  │  审稿意见     │          │
│  │  [文件上传]   │  │  [文件上传]   │          │
│  │  [文本框]    │  │  [文本框]    │          │
│  └──────────────┘  └──────────────┘          │
├──────────────────────────────────────────────┤
│  ③ 操作区                                    │
│  [加载示例数据]        [生成回复信 →]          │
├──────────────────────────────────────────────┤
│  ④ 输出区（双 Tab）                           │
│  ┌─ Tab: 回复信 ──┬─ Tab: 修改对照表 ──┐      │
│  │  回复信全文     │  两栏对照表          │      │
│  │  [可编辑文本框] │  [可编辑文本框]     │      │
│  │  [复制回复信]   │  [复制对照表]       │      │
│  └────────────────┴────────────────────┘      │
├──────────────────────────────────────────────┤
│  ⑤ Footer                                    │
│  隐私安全 · AI辅助 · 中英文支持 · 极速生成       │
└──────────────────────────────────────────────┘
```

### 5.2 输入模块

#### 论文原文输入

**UI 元素**：

| 元素 | 位置 | 说明 |
|------|------|------|
| 区域标签 | 卡片左上角 | 文案："论文原文"，字号 13px，字重 600，大写字母间距，颜色 #78716c |
| 文件上传 | 卡片内顶部 | 按钮文案："上传 PDF / Word / Markdown"，支持拖拽，接受 .pdf .docx .doc .txt .md |
| 文本框 | 文件上传下方 | 标签："或直接粘贴正文"，placeholder："支持 PDF、Word、Markdown 或纯文本上传，也可以直接粘贴论文正文到此处。建议粘贴完整内容：摘要、引言、方法、实验、结论等章节。" |
| 字符统计 | 文本框右下角 | 输入后实时显示当前字符数，阈值提示：< 100 字符显示橙色警告"内容偏短"，>= 100 字符显示灰色"已输入 N 字符" |

**交互状态**：

| 状态 | 表现 |
|------|------|
| 默认 | 空白文本框 + 文件上传区域显示虚线边框 |
| 文件拖入 | 虚线边框变为实线 + 颜色加深至 #a8a29e |
| 文件上传中 | 文件上传区域显示文件名 + 解析进度条（约 1-3 秒） |
| 文件解析完成 | 文本框自动填入提取的文本内容，上方显示蓝色提示条 `[已解析 PDF，共 12,345 字符]` |
| 文件解析失败 | 文本框显示错误信息 `[解析失败] 原因：xxx`，不阻断用户手动粘贴 |
| 用户手动粘贴 | 覆盖文件解析的内容，字符统计实时更新 |

**支持的文件格式与解析策略**：

| 格式 | 扩展名 | 解析方式 |
|------|--------|---------|
| PDF | .pdf | pdfplumber 提取文本（优先），解析失败时降级为 PyPDF2 |
| Word | .docx | python-docx 提取段落文本 |
| Markdown | .md | 直接读取文件原始文本 |
| 纯文本 | .txt | 直接读取文件原始文本 |
| Word (旧版) | .doc | 不支持，提示用户另存为 .docx 或直接粘贴 |

**校验规则**：

| 规则 | 触发时机 | 提示文案 |
|------|---------|---------|
| 论文为空 | 点击生成 | "请先上传论文 — 粘贴正文或上传 PDF / Word 文件均可。" |
| 论文 < 100 字符 | 点击生成 | "论文内容太短 — 请确保上传了完整的论文正文（至少包含摘要、引言、方法等核心章节）。" |
| 论文 > 50000 字符 | 点击生成 | "论文内容较长（N 字符），生成时间可能延长，建议仅保留核心章节。"（警告但不阻断） |

---

#### 审稿意见输入

**UI 元素**：

| 元素 | 位置 | 说明 |
|------|------|------|
| 区域标签 | 卡片右上角 | 文案："审稿意见"，样式同论文标签 |
| 文件上传 | 卡片内顶部 | 按钮文案："上传 PDF / Word / Markdown"，支持拖拽，接受 .pdf .docx .doc .txt .md |
| 文本框 | 文件上传下方 | 标签："或直接粘贴审稿意见"，placeholder："支持 PDF、Word、Markdown 或纯文本上传，也可以直接粘贴审稿意见原文。可以是：期刊审稿邮件、投稿系统导出意见、导师批注等。" |
| 字符统计 | 文本框右下角 | 输入后实时显示当前字符数 |

**校验规则**：

| 规则 | 触发时机 | 提示文案 |
|------|---------|---------|
| 审稿意见为空 | 点击生成 | "请先粘贴审稿意见 — 将期刊或会议的审稿意见邮件原文粘贴到右侧输入框。" |

**备注**：审稿意见没有最低字符数限制，因为有些期刊的审稿意见可能非常简短（如"论文可接受"）。

---

### 5.3 示例数据加载

**UI 元素**：

| 元素 | 位置 | 说明 |
|------|------|------|
| 按钮 | 生成按钮左侧 | 文案："加载示例数据"，variant 为 secondary，尺寸 sm |

**交互行为**：

| 步骤 | 说明 |
|------|------|
| 点击 | 同时向论文文本框和审稿意见文本框填入预设的示例内容 |
| 填入后 | 两个文本框的字符统计自动更新 |
| 覆盖行为 | 如果文本框中已有内容，直接覆盖（不弹确认框） |
| 可重复点击 | 每次点击都会重新填入相同的示例数据 |

**示例数据要求**：
- 示例论文：约 600-800 字，包含摘要、引言、方法、实验、结论五个章节
- 示例审稿意见：约 300-500 字，包含 2 位审稿人共 8 条意见，涵盖文献更新、方法细节、实验补充、写作改进四种常见类型
- 示例数据硬编码在前端，不依赖 API 调用

---

### 5.4 生成模块

#### 生成按钮

**UI 元素**：

| 元素 | 位置 | 说明 |
|------|------|------|
| 按钮 | 输入区下方，居中 | 文案："生成回复信"，variant 为 primary，size 为 lg，宽度填满操作行 |

**状态流转**：

```
[默认态] → [加载中] → [完成态] → [默认态]
   │                      │
   └──── [出错态] ←───────┘
```

| 状态 | 按钮表现 | 说明 |
|------|---------|------|
| 默认态 | 深色背景 (#1c1917)，白色文字，可点击 | 用户输入完成后 |
| Hover | 背景变亮 (#292524)，上浮 1px，阴影加深 | — |
| 加载中 | 按钮禁用（灰色），文字替换为旋转加载图标 + "正在生成..."，不可重复点击 | 禁止用户在生成期间再次点击 |
| 完成态 | 恢复默认态 | 生成成功或失败后均可再次点击 |
| 出错态 | 恢复默认态 + 输出区显示错误信息 | 详见 5.11 |

#### 生成进度

生成过程分为四个阶段，在输出区上方实时显示：

```
正在解析审稿意见...   ✓ 解析完成（识别到 8 条意见，分属 2 位审稿人）
正在匹配论文段落...   ✓ 段落匹配完成
正在生成回复草稿...   ✓ 回复草稿生成完成
正在整理修改对照表...  ✓ 对照表整理完成
```

**实现方式**：Gradio 不支持服务端推送进度，使用 gr.Info / gr.Warning toast 或输出区占位文字显示当前阶段。每完成一个阶段更新一次占位文字，最后一个阶段完成后替换为实际输出内容。

**生成总耗时预期**：15-45 秒（取决于论文和意见长度）。

#### 生成成功

- 输出区自动切换到"回复信"标签页
- 页面平滑滚动到输出区顶部
- 回复信全文和修改对照表同时渲染完成

#### 生成失败

- 输出区显示错误信息（格式详见 5.11）
- 按钮恢复可点击状态，用户可修改输入后重试
- 不自动滚动页面

---

### 5.5 输出模块

#### 整体布局

输出区使用**双标签页**组织，位于操作区下方，以分割线与输入区隔开：

| 元素 | 说明 |
|------|------|
| 标签一 | "回复信 Response Letter" |
| 标签二 | "修改对照表 Change Table" |
| 默认展示 | 生成完成后自动切换到标签一 |

每个标签页内包含：一个可编辑文本框 + 一个复制按钮。

---

#### 标签一：回复信 Response Letter

**输出格式规范**：

回复信由 LLM 按照以下固定结构生成，以 Markdown 格式呈现：

```
尊敬的编辑和审稿人：

感谢您对稿件提出的宝贵意见。我们已逐条认真考虑并修改如下。

---

【Reviewer 1】

**意见 1**：（引用审稿意见原文摘要）

**回复**：感谢您的建议。（具体说明修改了什么、怎么改的、在论文中哪个位置）

**意见 2**：（引用审稿意见原文摘要）

**回复**：感谢指出这一问题。根据建议，我们已在第三节补充了……
---
【Reviewer 2】

**意见 6**：……

**回复**：……
---
再次感谢编辑和审稿人的时间和宝贵意见。我们希望修改后的稿件已达到发表标准。

此致敬礼
```

**格式要点**：
- 开头致谢段落（固定模板）
- 按审稿人分组（Reviewer 1 / Reviewer 2 …）
- 每条意见前有序号，与原始意见编号对应
- 每条回复包含三要素：感谢或认可 + 具体修改说明 + 论文位置标注
- 结尾致谢段落（固定模板）
- 语言自动匹配：审稿意见为英文则生成英文回复信，中文意见生成中文回复信

**UI 元素**：

| 元素 | 位置 | 说明 |
|------|------|------|
| 文本框 | 标签页内 | 18-28 行高，可编辑，等宽字体不适用的长文本场景用系统默认字体，字号 14px，行高 1.7 |
| 复制按钮 | 文本框右下角 | 文案："复制回复信"，variant 为 secondary，size 为 sm |

---

#### 标签二：修改对照表 Change Table

**输出格式规范**：

修改对照表以 Markdown 表格格式呈现：

```
| # | 审稿意见要点 | 修改位置 | 修改内容摘要 | 状态 |
|---|------------|---------|------------|------|
| 1 | 文献综述需补充2024-2025年最新工作 | 第二节·相关工作 | 新增第3-5段，补充VideoMAE、UniFormer等2024年方法 | ✓ |
| 2 | 跨模态注意力机制缺少数学公式 | 第三节·方法 | 新增公式(3)-(5)，详细推导注意力权重计算过程 | ✓ |
| 3 | 实验遮挡条件缺少定量描述 | 第四节·实验·4.1数据集 | 新增表1，列出三种遮挡等级的定义与示例 | ⚠ 建议标注页码 |
| … | … | … | … | … |
```

**表格五列说明**：

| 列 | 内容 | 来源 |
|----|------|------|
| # | 审稿意见序号，与原始意见编号保持一致 | 意见解析阶段提取 |
| 审稿意见要点 | 每条意见的一句话摘要（不超过30字） | 意见解析阶段提取 |
| 修改位置 | 论文中对应的章节段落，如"第三节·方法""新增表2" | LLM 根据论文原文定位 |
| 修改内容摘要 | 实际做了什么修改（1-2句话，不超过60字） | LLM 生成 |
| 状态 | ✓ 已标注位置 / ⚠ 建议标注页码（见5.7质量提示） | 质量检查逻辑判定 |

**UI 元素**：

| 元素 | 位置 | 说明 |
|------|------|------|
| 文本框 | 标签页内 | 18-28 行高，可编辑，字号 14px，Markdown 表格以纯文本形式展示和编辑 |
| 复制按钮 | 文本框右下角 | 文案："复制对照表"，variant 为 secondary，size 为 sm |

---

### 5.6 修改对照表生成逻辑

修改对照表不是回复信的附属品，而是独立的功能模块。后端处理逻辑如下：

1. **审稿意见解析**：LLM 将原始审稿意见文本拆解为 `[{reviewer: "Reviewer 1", number: 1, content: "原文摘要", category: "literature/method/experiment/writing/minor"}]` 结构化列表
2. **论文段落匹配**：针对每条意见的 category 和 content，在论文原文中搜索最相关的章节段落
3. **回复生成**：逐条生成回复内容（包含致谢+修改说明+位置标注）
4. **对照表组装**：将 1 和 3 的结果组装为五列对照表
5. **质量检查**：检查每条回复是否包含修改位置标注、语气是否合适、回复是否完整（详见 5.7）

LLM 系统指令中需要明确要求输出两部分内容：
- Part A：完整回复信（标签一的内容）
- Part B：修改对照表（标签二的内容，严格按五列 Markdown 表格格式输出）

---

### 5.7 回复质量提示（P1）

**功能说明**：对每条生成的回复进行三项自动检查，结果以轻量标记形式展示。

**三项检查维度**：

| 检查项 | 通过标准 | 未通过标记 |
|--------|---------|-----------|
| 位置标注 | 回复中明确提到了论文的章节、段落、图表或页码 | ⚠ 建议标注修改位置 |
| 语气适宜 | 语气谦逊专业，无过度谄媚或强硬拒绝的表达 | ⚠ 语气可能偏高 / ⚠ 语气可能偏硬 |
| 内容完整 | 回复内容完整回应了审稿意见的所有要点（非选择性回复） | ⚠ 建议补充对 XX 要点的回应 |

**展示方式（MVP 降级方案）**：

由于 Gradio 文本框不支持行内标记，MVP 阶段采用"**对照表第五列 + 回复信文末汇总**"方式展示：

1. **修改对照表第五列**（状态列）：每条意见对应一行的状态标记，通过项显示 ✓，未通过项显示 ⚠
2. **回复信末尾追加质量摘要**：

```
---
### 质量提示

以下内容由系统自动检查生成，仅供参考：

- ⚠ 第 3 条回复：建议补充修改位置的页码信息（当前仅标注了"第四节"）
- ⚠ 第 7 条回复：语气可能偏硬，建议在拒绝审稿意见前先感谢对方的角度
- ✓ 其余 6 条回复通过基本检查
```

**质量检查的实现方式**：通过 LLM 在生成回复信的同时输出质量检查结果（JSON 格式），前端解析后渲染为对照表状态列和文末摘要。不单独调用一次 LLM 做质量检查以控制延迟和成本。

---

### 5.8 一键复制

**UI 元素**：

| 元素 | 位置 | 文案 | 状态 |
|------|------|------|------|
| 回复信复制按钮 | 标签一·文本框右下 | "复制回复信" | 默认 / 已复制 |
| 对照表复制按钮 | 标签二·文本框右下 | "复制对照表" | 默认 / 已复制 |

**交互行为**：

| 行为 | 说明 |
|------|------|
| 点击复制 | 将对应文本框的全部内容复制到系统剪贴板 |
| 复制成功 | 按钮文案切换为"✓ 已复制"，1.5 秒后恢复为默认文案 |
| 复制失败 | 若浏览器不支持 Clipboard API，按钮变灰并显示 tooltip"请手动选中文本后 Ctrl+C 复制" |

**实现方案**：前端 JavaScript 调用 `navigator.clipboard.writeText()`。因 Gradio 不直接支持此交互，通过在输出区下方嵌入 `<script>` 标签或使用 Gradio 的 `js` 参数实现。

---

### 5.9 结果编辑

**功能说明**：用户可直接在输出文本框内修改生成的回复信和对照表内容，确认无误后再复制使用。

**约束**：
- 纯文本编辑，不支持富文本（加粗、斜体等格式通过 Markdown 语法手动输入）
- 编辑不会触发重新生成
- 编辑后点击复制按钮，复制的是编辑后的内容
- 若用户编辑后再次点击"生成回复信"，原编辑内容会被新生成结果覆盖（不保留编辑历史）

**实现方式**：Gradio Textbox 组件默认可编辑（`interactive=True`），无需额外开发。

---

### 5.10 满意度评价

**功能说明**：生成完成后，在输出区底部展示满意度评价组件，收集用户反馈用于验证 PRD 指标一（满意度 ≥ 60%）。

**UI 元素**：

| 元素 | 位置 | 说明 |
|------|------|------|
| 评价提示 | 输出区底部 | 文案："这次生成结果对你有帮助吗？" |
| 三个按钮 | 提示下方，水平排列 | "👍 有用" / "😐 一般" / "👎 无帮助" |
| 补充反馈 | 按钮下方（点击 👎 后展开） | 可选文本框，placeholder："方便告诉我们哪里不够好吗？"（限 200 字） |

**交互行为**：

| 行为 | 说明 |
|------|------|
| 点击任意评价按钮 | 三个按钮同时置灰（不可重复评价），选中项高亮 |
| 点击 👎 | 展开可选反馈文本框 |
| 提交反馈 | 评价结果和可选反馈发送到后端日志 |
| 不评价 | 不阻断用户继续使用，评价组件始终可见且可选 |

**数据收集**：评价结果通过简单的后端日志记录（打印到控制台或写入日志文件），MVP 阶段不接数据库。对应埋点事件 `feedback_submit`。

---

### 5.11 错误与边界情况

| 错误场景 | 触发条件 | 用户看到的内容 | 用户可做什么 |
|---------|---------|--------------|------------|
| 论文为空 | 论文文本框为空或仅含空白字符就点击生成 | 输出区显示红色提示："请先上传论文——粘贴正文或上传 PDF / Word 文件均可。" | 上传文件或粘贴论文 |
| 论文太短 | 论文 < 100 字符 | 输出区显示红色提示："论文内容太短——请确保上传了完整的论文正文（至少包含摘要、引言、方法等核心章节）。" | 补充论文内容 |
| 审稿意见为空 | 审稿意见文本框为空 | 输出区显示红色提示："请先粘贴审稿意见——将期刊或会议的审稿意见邮件原文粘贴到右侧输入框。" | 上传文件或粘贴意见 |
| 论文偏长 | 论文 > 50000 字符 | 不阻断，仅显示黄色警告 toast："论文内容较长，生成可能需要更长时间，请耐心等待。" | 可选择缩减内容后重试 |
| API Key 未配置 | 环境变量 DEEPSEEK_API_KEY 未设置 | 输出区显示红色提示："API Key 未配置。请设置 DEEPSEEK_API_KEY 环境变量后重试。" | 联系管理员或自行配置 |
| API 调用超时 | LLM 请求超过 60 秒无响应 | 输出区显示红色提示："生成超时——API 响应时间过长，请稍后重试。如持续失败，可能是论文内容过长，建议缩减后重试。" | 缩减内容或稍后重试 |
| API 返回错误 | LLM 返回非 200 状态码 | 输出区显示红色提示："生成失败（错误码：xxx）。请稍后重试。" | 点击生成按钮重试 |
| 文件解析失败 | PDF/Word 文件损坏或加密 | 文本框显示 `[解析失败] 原因：xxx`，不阻断用户手动粘贴 | 手动粘贴论文内容 |
| 审稿意见无法解析 | 审稿意见文本不含可识别的意见条目（如纯闲聊文本） | 输出区显示："未能从审稿意见中识别到明确的问题条目。请确认粘贴的内容是审稿意见原文。" | 检查并重新粘贴 |
| 生成结果不完整 | LLM 返回的内容被截断（达到 max_tokens 限制） | 输出正常展示，末尾追加黄色提示："注意：生成内容可能不完整，建议检查最后一条回复是否完整。" | 编辑补充或重新生成 |

### 5.12 中英对照模式（P1，MVP 不做）

**功能预留说明**：当审稿意见原文为英文时，用户可选择开启"中英对照模式"。开启后，回复信以双语展示：每段先显示中文版本（用户确认内容准确性），再显示英文版本（用户检查措辞地道性）。修改对照表同样以双语展示。

MVP 阶段不做此功能，但以下设计决策需提前确认以免后续返工：
- 语言检测逻辑：根据审稿意见正文的语言自动判断（英文意见 → 英文回复），不提供手动切换
- 中英对照模式下，后端一次调用生成双语结果（而非调用两次 LLM）

### 5.13 已有功能兼容

| 场景 | 处理方式 |
|------|---------|
| 原有终端版论文生成系统 | 与新产品 ReviewResponse 功能独立，互不影响。原有 main.py 和 src 目录保留，供原有终端用户继续使用。新产品代码通过独立的 app.py 和 src/review_response.py 承载 |
| 原有 DeepSeek API 配置 | 复用现有 .env 文件中的 API Key 配置，不需要用户重新设置 |
| 原有学术搜索工具模块 | MVP 阶段不使用学术搜索功能，但保留 src/tools 目录代码，供后续版本扩展文献引用验证等功能时复用 |

---

## 六、数据埋点

| 触发条件 | 埋点事件 | 埋点参数 | 涉及终端 |
|---------|---------|---------|---------|
| 用户打开页面 | page_view | 页面来源（直接访问、分享链接）、时间戳 | Web |
| 用户上传论文文件 | paper_upload | 文件格式（PDF、Word、MD、TXT）、文件大小（字符数） | Web |
| 用户点击生成按钮 | generate_click | 是否需要登录（MVP 无此字段，预留）、论文字符数、审稿意见字符数 | Web |
| 生成成功 | generate_success | 生成耗时（毫秒）、回复信字符数、审稿意见解析条数 | Web |
| 生成失败 | generate_error | 错误类型（论文太短、审稿意见为空、API 调用失败等）、错误信息 | Web |
| 用户点击复制按钮 | copy_click | 复制类型（回复信、对照表） | Web |
| 用户编辑结果内容 | result_edit | 编辑类型（回复信、对照表）、编辑字符数 | Web |
| 用户点击满意度评价 | feedback_submit | 评价结果（有用、一般、无帮助）、可选文字反馈 | Web |
| 用户点击加载示例 | example_load | 触发来源（页面提示、主动点击） | Web |

---

## 七、设计参考

### 7.1 设计方向

MVP 阶段使用 Gradio 组件 + 自定义 CSS 实现，不做 Figma 高保真设计。

**当前原型**：[app.py](app.py)，已部署至 [HuggingFace Spaces](https://huggingface.co/spaces/Na-11/review-response)

**配色系统**（V2 更新，2026-05-15）：

| 角色 | 色值 | 说明 |
|------|------|------|
| 页面底色 | #fafaf8 | 暖白，偏暖微灰 |
| 卡片底色 | #ffffff | 纯白 |
| 卡片边框 | #e7e5e4 | 浅暖灰 |
| 卡片阴影 | `0 1px 3px rgba(0,0,0,0.04), 0 4px 12px rgba(0,0,0,0.04)` | 双层柔和阴影 |
| 主按钮底色 | #1c1917 | 暖黑，近黑色带暖调 |
| 主按钮悬停 | #292524 | 稍亮的暖黑 |
| 主文字 | #292524 | 深暖灰 |
| 次要文字 | #78716c | 中暖灰 |
| 输入框底色 | #fafaf8 | 与页面底色一致 |
| 输入框边框 | #e7e5e4 | 浅暖灰 |

**设计原则**：
- 暖石灰基调，避免深蓝色的沉闷感，保持学术感但不压抑
- 卡片式布局，多层阴影营造深度
- 大量留白和清晰的视觉层级
- 12-16px 圆角半径，柔和但不幼稚
- 按钮悬停微动效（上浮 1px + 阴影加深）
- 交互元素过渡时间 0.15-0.2s

**字体层级**：
- 页面标题：32px / weight 700 / letter-spacing -0.5px
- 区域标签：13px / weight 600 / 大写 / letter-spacing 0.8px
- 正文：14-15px / line-height 1.6-1.7
- 辅助文字：12px

### 7.2 与竞品设计差异

| 竞品 | 交互模式 | ReviewResponse |
|------|---------|---------------|
| ChatGPT | 对话式、非结构化 | 工具式、三步流程、结构化输出 |
| Paperpal | 实时侧边栏、逐句检查 | 异步提交-审核模式、整篇生成 |
| Grammarly | 句子级修正、下划线标注 | 结构化文档生成、五列对照表 |

---

## 附录

### 相关文档

1. 用户调研分析报告：[用户调研分析报告.md](用户调研分析报告.md)
2. 用户调研问卷：[用户调研问卷_审稿意见回复痛点.md](用户调研问卷_审稿意见回复痛点.md)
3. 竞品分析：[竞品分析_填充版.md](竞品分析_填充版.md)
4. 学术写作全流程痛点图谱：[学术写作全流程痛点图谱.md](学术写作全流程痛点图谱.md)
5. 竞品清单：[竞品清单.md](竞品清单.md)
6. 用户画像：见用户调研分析报告第七章和用户调研填充版第五节
7. 当前原型：[app.py](app.py)
8. 学术搜索工具模块（复用）：[src/tools/academic_search.py](src/tools/academic_search.py)
