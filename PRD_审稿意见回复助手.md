# 产品需求文档：审稿意见回复助手 ReviewResponse

> 版本：V1.1 MVP · 更新时间：2026年5月16日 · 需求负责人：YN

---

## 一、需求介绍

| 字段 | 内容 |
|------|------|
| 所属业务线 | AI 学术写作工具 |
| 需求负责人 | YN |
| 需求介绍 | 基于大语言模型，为有投稿经验的研究生和青年研究者提供审稿意见辅助回复工具。用户上传论文原文并粘贴审稿意见后，系统自动解析意见条目、生成逐条回复草稿并提供修改对照表。MVP 阶段为 Web 网页形态，核心验证三个假设：用户是否愿意使用专用工具替代 ChatGPT 做审稿回复、修改对照表功能是否足够差异化、愿意付费的用户比例是否与调研一致。 |

---

## 二、需求背景

### 2.1 需求价值

全球每年发表论文约 500 万篇，投稿量约为发表量的 2 到 3 倍，意味着每年有超过 1000 万次"收到审稿意见然后撰写回复信"的场景发生。非英语母语研究者占全球科研人员的百分之六十以上，中国研究生超 300 万。

用户调研（n=33）显示：百分之九十四的用户已经在用 ChatGPT 做审稿回复，但百分之六十七的用户认为其输出"一眼假，不敢用"。问题不在于 AI 写不出文字，而在于通用大模型没有论文上下文、不懂审稿回复的专业格式和语气规范、用户自己也无法判断生成结果的好坏。百分之七十九的用户明确表示需要一款专用工具，百分之七十愿意付费。

做审稿回复这个方向，不是跟 ChatGPT 比谁的文字生成能力更强，而是在它的盲区做深度：论文上下文感知、修改位置自动标注、学术语气内置保证、回复质量提示。

### 2.2 目标用户

**核心用户**：有第一作者或通讯作者投稿经历的硕士研究生和博士研究生，刚经历或正在经历第一次到第三次修回流程。学术写作经验不足，审稿回复信写作规范尚未建立，当前依赖 ChatGPT 加师兄师姐模板的手工拼接方案。

**次要用户**：有稳定投稿经验的博士后和青年教师，已有成熟的审稿回复习惯，对效率提升有需求但痛点不如核心用户强烈。

**非目标用户**：尚未投过稿的本科生，无修回经历，不在早期推广范围。

### 2.3 典型使用场景

**场景一：第一次投稿的硕士收到 Minor Revision**

研二学生小张投了一篇中文核心期刊，收到三周后返回的 Minor Revision，审稿意见共五条。小张打开 ChatGPT 把意见粘贴进去让它生成回复，但生成的文字"感觉很假"，也不知道回复信的格式对不对。她找到学姐要了一份过往的回复信模板，对照着改写，前前后后花了一个下午。她最担心的是"回复写得不够好，审稿人直接拒了"。

在这个场景中，ReviewResponse 的价值是：她只需上传论文和粘贴审稿意见，系统自动拆解意见、生成符合学术规范的逐条回复草稿，并告诉她每条回复的语气是否合适、修改位置是否已标注。她不需要自己会写 prompt，不需要找学姐要模板，也不需要反复纠结"写得对不对"。

**场景二：被导师反复退回修改的博士**

博三学生小李投了一篇 SCI 期刊，收到 Major Revision 共十二条审稿意见。他花了三天改完论文，又花了一天写回复信。导师看了回复信草稿不满意，让他逐条修改措辞和补充理由。他反复改了五六版，每次都要重新标注哪些修改对应论文的哪个位置。最崩溃的是改到后期已经搞不清 v3 和 v5 的区别了。

在这个场景中，ReviewResponse 的价值是：系统生成回复草稿的同时自动输出修改对照表，左栏是审稿意见要点，右栏标注论文中对应的修改段落位置。导师或合作者可以直接在表上审批，无需对着论文原文查找。

**场景三：英文期刊的回复焦虑**

研二学生小王第一次投英文期刊，审稿意见全是英文。虽然大致看得懂，但写英文回复信时完全拿不准语气——怕太随意显得不专业，又怕太正式显得僵硬。她用 DeepL 辅助写作，但翻译出来的表达有时很生硬，自己又判断不了。

在这个场景中，ReviewResponse 的价值是：系统内置学术英文回复的语气规范，生成中英对照版回复信，用户可以先确认中文内容准确无误，再查看英文表达是否地道。系统还可标注出可能导致语气问题的措辞，提醒用户斟酌。

### 2.4 竞品分析

#### 2.4.1 分析结论

1. 审稿回复辅助是一个品类空白。目前没有发现任何产品专门做"审稿意见到逐条回复"这件事。最接近的 Paperpal 在审稿回复环节仅做格式检查层面的简单校验，不生成回复内容。
2. 用户当前默认方案是 ChatGPT（使用率百分之九十四），但满意度低。两个核心缺口：一是 ChatGPT 没有论文原文上下文，用户需要在两个窗口间反复对照；二是 ChatGPT 不了解审稿回复信的格式规范和语气要求，用户需要自己设计 prompt 作文本引导。
3. 不跟 ChatGPT 比生成能力，比它做不到的事：论文段落自动匹配、学术语气内置、修改位置标注、回复质量提示。
4. 进入门槛不高（MVP 核心技术是 LLM prompt 加论文段落匹配），长期壁垒在于持续积累的审稿回复语料和用户行为数据。

#### 2.4.2 竞品方案

目前审稿回复场景不存在专用竞品。用户当前方案为 ChatGPT 加手工拼接：将审稿意见粘贴进 ChatGPT，通过手动设计的 prompt 引导生成回复。然后自己整理格式，对照论文原文逐条检查修改位置是否正确标注，最后参考过往回复信模板调整语气和措辞。整个流程涉及多个工具切换，核心痛点是自己对照的机械劳动和自己判断质量的认知压力。

Paperpal 定位投稿前全流程平台，审稿回复仅为其二十余个功能中的小模块，功能是检查已写好的回复信格式是否合规，不生成回复内容。两者不存在直接竞争。

---

## 三、需求目标

### 3.1 需求目标

1. 验证用户是否愿意从 ChatGPT 迁移到专用工具完成审稿回复任务
2. 验证修改对照表功能是否构成足够的差异化壁垒
3. 积累首批用户的行为数据和反馈，为 V2 版本的功能决策提供依据
4. 跑通"上传到生成到审核"的完整闭环，建立可迭代的产品基线

### 3.2 衡量指标

| 指标 | 计算方式 | 目标值 |
|------|---------|--------|
| 用户满意度 | 生成结果后下发满意度评价（有用、一般、无帮助） | 有用比例不低于百分之六十 |
| 生成完成率 | 成功生成回复信的次数除以点击生成按钮的次数 | 不低于百分之九十 |
| 用户留存 | 一周内使用过两次及以上的用户数除以总用户数 | 不低于百分之三十 |
| 分享转化 | 通过分享链接访问的新用户数占总访问用户数的比例 | 不低于百分之二十 |
| 平均使用时长 | 从打开页面到关闭页面的平均停留时间 | 不低于三分钟 |

---

## 四、功能蓝图

每个功能必须能回答四个问题：**做什么（功能）、为什么做（目的）、凭什么做（依据）、怎么算做完（验收）**。

### 4.1 功能总览表

| # | 功能 | 目的 | 调研依据 | 优先级 |
|---|------|------|---------|:--:|
| F1 | 论文原文上传 | 让系统读懂论文，消除 ChatGPT 模式下用户手动对照的机械劳动 | Q8 困难度均值 3.76/5；66.7% 抱怨需反复对照 | P0 |
| F2 | 审稿意见输入 | 接收审稿意见原文，作为生成回复的输入源 | 94% 用户已在用 ChatGPT 粘贴审稿意见，行为习惯已验证 | P0 |
| F3 | 回复信生成 | 自动生成符合学术规范的逐条回复草稿，用户审核后可直接提交 | 66.7% 用户觉 AI 回复"一眼假不敢用"——根因是格式语气不对、没上下文；63.6% 需要逐条回复草稿 | P0 |
| F4 | 修改对照表 | 让用户和导师一眼看到每条意见改了什么、改在哪 | 66.7% 需要自动生成修改对照表；场景二：博士反复改五六版搞不清版本 | P0 |
| F5 | 一键复制 | 将回复信和对照表快速复制到剪贴板，粘贴到投稿系统 | 现有替代方案（ChatGPT）均支持复制，属于基础预期 | P0 |
| F6 | 加载示例数据 | 零门槛体验，新用户无需输入即可看到完整效果 | 支撑"分享转化 ≥20%"指标 | P0 |
| F7 | 回复质量提示 | 告诉用户每条回复哪里可能有问题，解决"自己判断不了好不好"的焦虑 | 51.5% 抱怨通用 AI 不了解回复格式规范；场景一"担心回复不够好" | P1 |
| F8 | 结果编辑 | 用户可直接在输出框修改文字，确认无误后再复制使用 | 生成内容仅供参考，用户需最终审核 | P1 |
| F9 | 中英对照 | 英文审稿意见场景提供中英双语对照 | 场景三（英文期刊回复焦虑）；Q19 开放题用户自发提到需要参考 | P1 |

### 4.2 功能依赖关系

```
F1 论文上传 ──┐
              ├──→ F3 回复信生成 ──→ F4 修改对照表 ──→ F7 质量提示
F2 意见输入 ──┘         │                    │
                        └──→ F5 一键复制 ←───┘
                             F8 结果编辑
                             F9 中英对照 (P1)

F6 加载示例（独立，不依赖其他功能）
```

### 4.3 不做清单（P2 及以后）

| 项目 | 不做原因 |
|------|---------|
| 用户注册登录系统 | MVP 验证核心价值，注册体系增加开发和运维成本 |
| 历史生成记录保存 | 需要数据库，MVP 不做持久化 |
| 回复模板库 | 需要积累足够用户数据后才能建库 |
| 导师在线审批批注 | B 端功能，C 端 MVP 不覆盖 |
| 一键导出 Word / PDF | 复制粘贴可覆盖基础需求 |
| 移动端适配 | 学术写作主战场在桌面端 |
| 语法检查与润色 | Grammarly / Paperpal 已做得很好，不重复造轮子 |
| 期刊格式适配 | 各期刊格式要求差异大，MVP 做通用方案 |

---

## 五、功能详细规格

> 每个功能包含：目的 / 调研依据 / 原型规格 / 验收标准。

### 5.1 F1·论文原文上传

**目的**：让系统获取论文全文上下文，消除用户在 ChatGPT 和论文窗口之间手动对照的机械劳动。

**调研依据**：66.7% 用户抱怨"需要自己在论文和意见间反复对照"；Q8 审稿回复困难度均值 3.76/5。

**UI 元素**：

| 元素 | 位置 | 说明 |
|------|------|------|
| 区域标签 | 左卡片左上角 | 文案"论文原文"，13px / weight 600 / 大写字母间距 / #78716c |
| 文件上传 | 左卡片内顶部 | 文案"上传 PDF / Word / Markdown"，支持拖拽，接受 .pdf .docx .txt .md |
| 文本框 | 文件上传下方 | 标签"或直接粘贴正文"，placeholder 提示支持格式和建议粘贴内容 |
| 字符统计 | 文本框右下角 | 实时显示字符数，< 100 字符橙色警告，≥ 100 字符灰色 |

**交互状态**：

| 状态 | 表现 |
|------|------|
| 默认 | 空白文本框 + 文件上传区虚线边框 |
| 文件拖入 | 虚线变实线，颜色加深至 #a8a29e |
| 文件解析中 | 显示文件名 + 解析进度（约 1-3 秒） |
| 解析完成 | 文本自动填入，上方蓝色提示 `[已解析 PDF，共 N 字符]` |
| 解析失败 | 显示 `[解析失败] 原因：xxx`，不阻断用户手动粘贴 |
| 用户手动粘贴 | 覆盖文件解析内容，字符统计更新 |

**支持格式**：

| 格式 | 扩展名 | 解析方式 |
|------|--------|---------|
| PDF | .pdf | pdfplumber 优先，降级 PyPDF2 |
| Word | .docx | python-docx 提取段落文本 |
| Markdown | .md | 直接读取原始文本 |
| 纯文本 | .txt | 直接读取原始文本 |
| Word (旧版) | .doc | 不支持，提示另存为 .docx |

**校验规则**：

| 规则 | 触发时机 | 提示文案 |
|------|---------|---------|
| 论文为空 | 点击生成 | "请先上传论文——粘贴正文或上传 PDF / Word 文件均可。" |
| 论文 < 100 字符 | 点击生成 | "论文内容太短——请确保上传了完整的论文正文（至少包含摘要、引言、方法等核心章节）。" |
| 论文 > 50000 字符 | 点击生成 | "论文内容较长（N 字符），生成时间可能延长。"（黄色警告，不阻断） |

**验收标准**：
- [ ] 支持 .pdf / .docx / .md / .txt 四种格式上传，单文件 ≤ 10MB
- [ ] PDF 解析成功率 ≥ 95%（测试 20 份常见学术 PDF）
- [ ] 文件上传后 3 秒内完成解析并填入文本框
- [ ] 解析失败时不阻断用户手动粘贴
- [ ] 论文为空或 < 100 字符时点击生成按钮被拦截并报错
- [ ] 论文 > 50000 字符时弹出警告但不阻断

---

### 5.2 F2·审稿意见输入

**目的**：接收审稿意见原文，作为 LLM 生成回复和对照表的输入源。

**调研依据**：94% 用户已在用 ChatGPT 粘贴审稿意见（行为习惯已验证）；51.5% 抱怨通用 AI 不了解回复格式规范。

**UI 元素**：

| 元素 | 位置 | 说明 |
|------|------|------|
| 区域标签 | 右卡片左上角 | 文案"审稿意见"，样式同论文标签 |
| 文件上传 | 右卡片内顶部 | 文案"上传 PDF / Word / Markdown"，支持拖拽 |
| 文本框 | 文件上传下方 | 标签"或直接粘贴审稿意见"，placeholder 提示可接受来源 |

**校验规则**：

| 规则 | 触发时机 | 提示文案 |
|------|---------|---------|
| 审稿意见为空 | 点击生成 | "请先粘贴审稿意见——将期刊或会议的审稿意见邮件原文粘贴到右侧输入框。" |

审稿意见无最低字符限制（极短意见如"论文可接受"允许生成）。

**验收标准**：
- [ ] 支持文件上传和文本粘贴两种输入方式
- [ ] 审稿意见为空时点击生成按钮被拦截并报错
- [ ] 文件解析失败时不阻断用户手动粘贴

---

### 5.3 F3·回复信生成

**目的**：基于论文全文和审稿意见，自动生成符合学术规范的逐条回复草稿，用户审核后可直接提交。

**调研依据**：66.7% 用户觉 AI 回复"一眼假不敢用"——根因不是文笔，是格式不对、没上下文、语气不对；63.6% 明确需要逐条回复草稿功能。

#### 5.3.1 页面布局总览

```
┌──────────────────────────────────────────────┐
│  ① Header                                    │
│  ReviewResponse ｜ 审稿意见回复，一步到位        │
├──────────────────────────────────────────────┤
│  ② 输入区（双卡片并排）                         │
│  ┌──────────────┐  ┌──────────────┐          │
│  │  论文原文     │  │  审稿意见     │          │
│  │  [文件上传]   │  │  [文件上传]   │          │
│  │  [文本框]    │  │  [文本框]    │          │
│  └──────────────┘  └──────────────┘          │
├──────────────────────────────────────────────┤
│  ③ 操作区                                    │
│  [加载示例数据]             [生成回复信 →]      │
├──────────────────────────────────────────────┤
│  ④ 输出区（双 Tab）                           │
│  ┌─ Tab: 回复信 ──┬─ Tab: 修改对照表 ──┐      │
│  │  回复信全文     │  五列对照表          │      │
│  │  [可编辑文本框] │  [可编辑文本框]     │      │
│  │  [复制回复信]   │  [复制对照表]       │      │
│  └────────────────┴────────────────────┘      │
│  [👍 有用] [😐 一般] [👎 无帮助]              │
├──────────────────────────────────────────────┤
│  ⑤ Footer                                    │
│  隐私安全 · AI辅助 · 中英文支持 · 极速生成       │
└──────────────────────────────────────────────┘
```

#### 5.3.2 生成按钮

| 元素 | 位置 | 说明 |
|------|------|------|
| 按钮 | 输入区下方居中 | 文案"生成回复信"，variant primary / size lg，宽度填满操作行 |

**状态流转**：

| 状态 | 按钮表现 |
|------|---------|
| 默认态 | 暖黑背景 #1c1917，白色文字，可点击 |
| Hover | 背景变亮 #292524，上浮 1px，阴影加深 |
| 加载中 | 禁用灰色，文字替换为旋转图标 + "正在生成..."，不可重复点击 |
| 完成态 | 恢复默认态，可再次点击 |
| 出错态 | 恢复默认态 + 输出区显示错误信息 |

#### 5.3.3 生成进度

四阶段占位文字在输出区展示：

```
正在解析审稿意见...   ✓ 解析完成（识别到 8 条意见，分属 2 位审稿人）
正在匹配论文段落...   ✓ 段落匹配完成
正在生成回复草稿...   ✓ 回复草稿生成完成
正在整理修改对照表...  ✓ 对照表整理完成
```

#### 5.3.4 输出区：标签一·回复信

输出格式（Markdown）：

```
尊敬的编辑和审稿人：

感谢您对稿件提出的宝贵意见。我们已逐条认真考虑并修改如下。

---

【Reviewer 1】

**意见 1**：（引用审稿意见原文摘要）

**回复**：感谢您的建议。（具体说明修改了什么、怎么改的、在论文中哪个位置）

---

再次感谢编辑和审稿人的时间和宝贵意见。

此致敬礼
```

格式要点：开头致谢 + 按审稿人分组 + 每条三要素（感谢 + 修改说明 + 位置标注）+ 结尾致谢。语言自动匹配意见语言。

| 元素 | 说明 |
|------|------|
| 文本框 | 18-28 行，14px / line-height 1.7，可编辑 |
| 复制按钮 | 文案"复制回复信"，variant secondary，size sm |

#### 5.3.5 生成成功/失败行为

- 成功后：自动切换到回复信标签页，页面滚动到输出区
- 失败后：显示错误信息，按钮恢复可点击，不自动滚动

**验收标准**：
- [ ] 回复信包含三个必要部分：开头致谢 + 按审稿人分组的逐条回复 + 结尾致谢
- [ ] 每条回复包含三要素：感谢/认可 + 具体修改说明 + 论文位置标注
- [ ] 语言自动匹配：英文意见 → 英文回复，中文意见 → 中文回复
- [ ] 生成耗时 15-45 秒（论文 ≤ 50000 字符）
- [ ] 点击生成后按钮进入加载状态，不可重复点击
- [ ] 生成完成后自动切换到回复信标签页

---

### 5.4 F4·修改对照表

**目的**：独立于回复信之外输出五列对照表，让用户和导师一眼看到每条意见改了什么、改在哪。解决场景二中"博士反复改五六版搞不清 v3/v5 区别"的版本混乱问题。

**调研依据**：66.7% 用户需要自动生成修改对照表；48.5% 需要自动定位论文对应段落。

**输出格式**（Markdown 表格）：

```
| # | 审稿意见要点 | 修改位置 | 修改内容摘要 | 状态 |
|---|------------|---------|------------|------|
| 1 | 文献综述需补充2024-2025年工作 | 第二节·相关工作 | 新增第3-5段，补充VideoMAE等方法 | ✓ |
| 2 | 跨模态注意力缺少数学公式 | 第三节·方法 | 新增公式(3)-(5) | ✓ |
| 3 | 实验遮挡条件缺少定量描述 | 第四节·4.1数据集 | 新增表1 | ⚠ 建议标注页码 |
```

**五列说明**：

| 列 | 内容 | 来源 |
|----|------|------|
| # | 审稿意见序号 | 意见解析阶段提取 |
| 审稿意见要点 | 每条意见一句话摘要（≤30字） | 意见解析阶段提取 |
| 修改位置 | 论文中对应章节段落 | LLM 根据论文原文定位 |
| 修改内容摘要 | 实际修改了什么（1-2句话，≤60字） | LLM 生成 |
| 状态 | ✓ 已标注位置 / ⚠ 建议标注页码 | 质量检查判定 |

**UI 元素**：

| 元素 | 说明 |
|------|------|
| 文本框 | 18-28 行，14px，可编辑 |
| 复制按钮 | 文案"复制对照表"，variant secondary，size sm |

**验收标准**：
- [ ] 对照表包含五列：# / 审稿意见要点 / 修改位置 / 修改内容摘要 / 状态
- [ ] "修改位置"列至少 80% 的行包含明确章节标注
- [ ] "状态"列使用 ✓ 和 ⚠ 标记，无空白行
- [ ] 对照表作为独立标签页展示，不与回复信混在一起
- [ ] 复制按钮仅复制对照表内容

---

### 5.5 F5·一键复制

**目的**：将回复信和对照表快速复制到剪贴板，粘贴到投稿系统或邮件中。

**调研依据**：用户现有方案（ChatGPT）均支持复制，属于基础预期。

**UI 元素**：

| 按钮 | 位置 | 文案 | 状态 |
|------|------|------|------|
| 回复信复制 | 标签一·文本框右下 | "复制回复信" | 默认 / ✓ 已复制 |
| 对照表复制 | 标签二·文本框右下 | "复制对照表" | 默认 / ✓ 已复制 |

**交互**：点击复制 → 按钮文案切换"✓ 已复制"→ 1.5 秒恢复。复制失败时按钮置灰 tooltip "请手动 Ctrl+C 复制"。

**验收标准**：
- [ ] 两个标签页各有独立复制按钮
- [ ] 点击后 1.5 秒内按钮反馈"已复制"并恢复
- [ ] 复制的是用户编辑后的最新内容
- [ ] 浏览器不支持 Clipboard API 时有降级提示

---

### 5.6 F6·加载示例数据

**目的**：零门槛体验，新用户不输入任何内容即可看到完整生成效果，降低首次使用摩擦。

**调研依据**：支撑 PRD 指标"分享转化 ≥ 20%"，示例数据是新用户判断产品价值的第一个触点。

**UI 元素**：按钮位于生成按钮左侧，文案"加载示例数据"，variant secondary / size sm。

**交互**：点击后同时填入示例论文（约 600-800 字，含五章节）和示例审稿意见（约 300-500 字，8 条意见分属 2 位审稿人）。直接覆盖已有内容不弹确认框。硬编码在前端，不发起 API 调用。可重复点击。

**验收标准**：
- [ ] 按钮在生成按钮左侧，variant 为 secondary
- [ ] 点击后同时填入论文和审稿意见，不弹确认框
- [ ] 硬编码在前端，不发起 API 调用
- [ ] 可重复点击

---

### 5.7 F7·回复质量提示（P1）

**目的**：告诉用户每条回复哪里可能有问题（语气、位置标注、完整性），解决"自己判断不了写得好不好"的焦虑。

**调研依据**：51.5% 抱怨通用 AI 不了解回复格式规范；场景一"最担心回复不够好审稿人直接拒了"。

**三项检查**：

| 检查项 | 通过标准 | 未通过标记 |
|--------|---------|-----------|
| 位置标注 | 回复中提到了论文章节/段落/图表/页码 | ⚠ 建议标注修改位置 |
| 语气适宜 | 谦逊专业，无过度谄媚或强硬拒绝 | ⚠ 语气可能偏高 / ⚠ 语气可能偏硬 |
| 内容完整 | 回复完整回应了意见的全部要点 | ⚠ 建议补充对 XX 要点的回应 |

**MVP 展示方案**：因 Gradio 文本框不支持行内标记，采用两种方式——
1. 对照表第五列（状态列）：通过 ✓ / ⚠ 标记
2. 回复信末尾追加质量摘要，列出需要关注的条目

LLM 在生成回复信的同时输出质量检查结果，不单独调用一次 LLM。

**验收标准**：
- [ ] 每条回复均检查三个维度
- [ ] 检查结果展示在对照表状态列和回复信末尾
- [ ] 无额外 LLM 调用，质量检查嵌入在同一次生成中

---

### 5.8 F8·结果编辑

**目的**：用户可直接在输出文本框内修改生成内容，确认无误后再复制使用。

**调研依据**：AI 生成内容仅供参考，用户需要最终审核修改。

**约束**：
- 纯文本编辑，不支持富文本
- 编辑不触发重新生成
- 复制的是编辑后内容
- 再次点击生成会覆盖编辑内容（不保留历史）

**验收标准**：
- [ ] 两个标签页的文本框均可编辑
- [ ] 复制按钮复制的是编辑后的最新内容

---

### 5.9 F9·中英对照模式（P1，MVP 不做）

**目的**：英文审稿意见场景下提供中英双语对照，中文确认内容、英文查看措辞。

**调研依据**：场景三（英文期刊回复焦虑）；Q19 开放题用户自发提到需要模板/参考。

**设计预留**：
- 语言检测逻辑：根据审稿意见语言自动判断，不提供手动切换
- 后端一次调用生成双语结果，不调用两次 LLM

---

### 5.10 满意度评价

**目的**：收集用户反馈，验证 PRD 指标一（满意度 ≥ 60%）。

**UI 元素**：

| 元素 | 说明 |
|------|------|
| 提示文字 | 输出区底部："这次生成结果对你有帮助吗？" |
| 三个按钮 | "👍 有用" / "😐 一般" / "👎 无帮助" |
| 补充反馈 | 点击 👎 后展开，placeholder："方便告诉我们哪里不够好吗？"（限 200 字） |

**交互**：
- 点击任意按钮后三项同时置灰（不可重复评价），选中项高亮
- 点击 👎 展开可选反馈文本框
- 评价结果写入后端日志（不接数据库）
- 不评价不阻断使用

**验收标准**：
- [ ] 生成完成后评价组件在输出区底部可见
- [ ] 三个按钮可点击且单选
- [ ] 点击 👎 后展开反馈文本框
- [ ] 评价后不可重复评价
- [ ] 对应埋点事件 feedback_submit

---

### 5.11 错误与边界情况

| 错误场景 | 触发条件 | 用户看到的内容 | 用户可做什么 |
|---------|---------|--------------|------------|
| 论文为空 | 文本框为空或仅空白字符 | 红色提示："请先上传论文——粘贴正文或上传 PDF / Word 文件均可。" | 上传文件或粘贴 |
| 论文太短 | 论文 < 100 字符 | 红色提示："论文内容太短——请确保上传了完整的论文正文。" | 补充内容 |
| 审稿意见为空 | 文本框为空 | 红色提示："请先粘贴审稿意见——将审稿意见邮件原文粘贴到右侧输入框。" | 上传或粘贴 |
| 论文偏长 | > 50000 字符 | 黄色 toast："论文内容较长，生成可能需要更长时间。" | 可选缩减 |
| API Key 未配置 | 环境变量缺失 | 红色提示："API Key 未配置。请设置 DEEPSEEK_API_KEY 环境变量。" | 联系管理员 |
| API 超时 | > 60 秒无响应 | 红色提示："生成超时——请稍后重试。" | 缩减或重试 |
| API 错误 | 非 200 状态码 | 红色提示："生成失败（错误码：xxx）。请稍后重试。" | 重试 |
| 文件解析失败 | 文件损坏或加密 | 文本框显示 `[解析失败] 原因：xxx` | 手动粘贴 |
| 意见无法解析 | 不含可识别意见条目 | "未能识别到明确的问题条目。请确认内容为审稿意见原文。" | 检查重试 |
| 生成结果截断 | 达到 max_tokens | 文末黄色提示："生成内容可能不完整，建议检查最后一条回复。" | 编辑补充 |

### 5.12 已有功能兼容

| 场景 | 处理方式 |
|------|---------|
| 原有终端版论文生成系统 | 与 ReviewResponse 功能独立，原有代码保留 |
| 原有 DeepSeek API 配置 | 复用 .env 中的 API Key |
| 原有学术搜索工具模块 | 保留代码，V2 扩展文献验证时复用 |

---

## 六、数据埋点

| 触发条件 | 埋点事件 | 埋点参数 | 涉及终端 |
|---------|---------|---------|---------|
| 用户打开页面 | page_view | 页面来源（直接访问、分享链接）、时间戳 | Web |
| 用户上传论文文件 | paper_upload | 文件格式、文件大小 | Web |
| 用户点击生成按钮 | generate_click | 论文字符数、审稿意见字符数 | Web |
| 生成成功 | generate_success | 生成耗时（ms）、回复信字符数、意见解析条数 | Web |
| 生成失败 | generate_error | 错误类型、错误信息 | Web |
| 用户点击复制按钮 | copy_click | 复制类型（回复信、对照表） | Web |
| 用户编辑结果内容 | result_edit | 编辑类型（回复信、对照表）、编辑字符数 | Web |
| 用户点击满意度评价 | feedback_submit | 评价结果（有用、一般、无帮助）、可选文字反馈 | Web |
| 用户点击加载示例 | example_load | 触发来源 | Web |

---

## 七、设计参考

### 7.1 设计方向

MVP 阶段使用 Gradio + 自定义 CSS，不做 Figma 高保真。

**当前原型**：[app.py](app.py)，已部署至 [HuggingFace Spaces](https://huggingface.co/spaces/Na-11/review-response)

**配色系统**（暖石灰·精炼黑）：

| 角色 | 色值 | 说明 |
|------|------|------|
| 页面底色 | #fafaf8 | 暖白 |
| 卡片底色 | #ffffff | 纯白 |
| 卡片阴影 | 双层柔和阴影 | 0 1px 3px + 0 4px 12px rgba |
| 主按钮 | #1c1917 | 暖黑，近黑带暖调 |
| 主按钮 hover | #292524 | 稍亮暖黑 |
| 主文字 | #292524 | 深暖灰 |
| 次要文字 | #78716c | 中暖灰 |
| 边框 | #e7e5e4 | 浅暖灰 |

**设计原则**：暖石灰基调（避免深蓝沉闷），卡片式布局多层阴影，大量留白，12-16px 圆角，按钮 hover 上浮 1px + 阴影加深。

### 7.2 与竞品交互差异

| 竞品 | 交互模式 | ReviewResponse |
|------|---------|---------------|
| ChatGPT | 对话式、非结构化 | 工具式、结构化输出 |
| Paperpal | 实时侧边栏、逐句检查 | 异步提交-审核模式 |
| Grammarly | 句子级修正、下划线 | 结构化文档生成、五列对照表 |

---

## 附录

### 相关文档

1. 用户调研分析报告：[用户调研分析报告.md](用户调研分析报告.md)
2. 用户调研问卷：[用户调研问卷_审稿意见回复痛点.md](用户调研问卷_审稿意见回复痛点.md)
3. 竞品分析：[竞品分析_填充版.md](竞品分析_填充版.md)
4. 学术写作全流程痛点图谱：[学术写作全流程痛点图谱.md](学术写作全流程痛点图谱.md)
5. 竞品清单：[竞品清单.md](竞品清单.md)
6. 用户画像：见用户调研分析报告第七章和用户调研填充版第五节
7. 当前原型：[app.py](app.py)
8. 学术搜索工具模块（复用）：[src/tools/academic_search.py](src/tools/academic_search.py)
